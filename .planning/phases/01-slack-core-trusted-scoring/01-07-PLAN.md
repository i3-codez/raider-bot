---
phase: 01-slack-core-trusted-scoring
plan: 07
type: execute
wave: 3
depends_on:
  - 01-03
  - 01-04
files_modified:
  - package.json
  - src/domain/raids/correct-raid-published-at.ts
  - src/db/queries/update-raid-published-at.ts
  - src/db/queries/insert-raid-timing-correction.ts
  - src/scripts/correct-raid-published-at.ts
  - tests/raids/correct-raid-published-at.test.ts
autonomous: true
requirements:
  - RAID-03
  - ENG-04
  - ENG-06
must_haves:
  truths:
    - "Correcting `published_at` recalculates engagement scores and updates the Slack timing row while preserving correction history."
    - "The timing-correction path is reachable from a CLI entry point with required flags."
    - "Corrected raids move to high confidence without reactivating removed engagement claims."
  artifacts:
    - path: src/domain/raids/correct-raid-published-at.ts
      provides: "Authoritative publish-time correction service"
      exports:
        - correctRaidPublishedAt
    - path: src/db/queries/update-raid-published-at.ts
      provides: "Canonical raid-update and engagement-recalculation query path"
      contains: "month_key"
    - path: src/db/queries/insert-raid-timing-correction.ts
      provides: "Durable audit insert for D-04 corrections"
      contains: "previous_published_at"
    - path: src/scripts/correct-raid-published-at.ts
      provides: "Reachable CLI entry point for D-04 corrections"
      contains: "--raid-id"
  key_links:
    - from: src/domain/raids/correct-raid-published-at.ts
      to: src/db/queries/update-raid-published-at.ts
      via: "authoritative timing update and engagement recalculation"
      pattern: "updateRaidPublishedAt\\("
    - from: src/domain/raids/correct-raid-published-at.ts
      to: src/db/queries/insert-raid-timing-correction.ts
      via: "correction audit persistence"
      pattern: "insertRaidTimingCorrection\\("
    - from: src/domain/raids/correct-raid-published-at.ts
      to: src/slack/blocks/build-raid-message.ts
      via: "chat.update with canonical blocks"
      pattern: "buildRaidMessage\\("
    - from: package.json
      to: src/scripts/correct-raid-published-at.ts
      via: "CLI script entry point"
      pattern: "correct:raid-time"
---

<objective>
Implement the D-04 timing-correction path that upgrades low-confidence raids to authoritative publish times, recalculates affected engagement facts, and refreshes the Slack timing row without losing history.

Purpose: The Phase 1 trust model stays credible only if late authoritative timing can repair approximate raids while preserving an auditable correction trail.
Output: Correction service, audit query helpers, executable CLI entry point, and tests for recalculation plus message refresh.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md
@AGENTS.md

<interfaces>
Expected upstream contracts from Plans 01-03 and 01-04:

From `src/domain/scoring/scoring-config.ts`:
```typescript
export const SCORING_WINDOWS: Array<{
  label: "0-10m" | "10-20m" | "20-30m" | "30-60m" | "60m+";
  minMinutes: number;
  maxMinutes: number | null;
  points: 10 | 8 | 6 | 3 | 0;
}>;
```

From `src/slack/blocks/build-raid-message.ts`:
```typescript
export function buildRaidMessage(input: {
  raid: RaidPost;
  showPoints: false;
}): {
  text: string;
  blocks: unknown[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement D-04 timing correction with recalculation and message update</name>
  <files>src/domain/raids/correct-raid-published-at.ts, src/db/queries/update-raid-published-at.ts, src/db/queries/insert-raid-timing-correction.ts, tests/raids/correct-raid-published-at.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, .planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md, src/lib/time.ts, src/domain/raids/types.ts, src/domain/scoring/scoring-config.ts, src/slack/blocks/build-raid-message.ts, src/domain/raids/correct-raid-published-at.ts, src/db/queries/update-raid-published-at.ts, src/db/queries/insert-raid-timing-correction.ts, supabase/migrations/20260410213000_phase1_core.sql</read_first>
  <behavior>
    - Test 1: correcting a low-confidence raid to an authoritative `published_at` inserts one `raid_timing_corrections` row with the previous and new timing values.
    - Test 2: correction recalculates every engagement row's `minutes_from_publish`, `scoring_window`, and `points_awarded` using the original `reacted_at`.
    - Test 3: correction preserves any existing `removed_at` values instead of reactivating removed claims.
    - Test 4: correction updates the Slack message using `buildRaidMessage` so the dedicated timing row changes from approximate to `Published: {timestamp} ET`.
  </behavior>
  <action>Implement `correctRaidPublishedAt({ raidPostId, publishedAt, correctedBy, client })` in `src/domain/raids/correct-raid-published-at.ts`. This is the concrete D-04 path: fetch the raid, record the previous `published_at` and `timing_confidence`, insert a row into `raid_timing_corrections` with `reason: "authoritative_publish_time"`, update the raid through `src/db/queries/update-raid-published-at.ts` so `published_at` becomes the new authoritative timestamp, `timing_confidence` becomes `"high"`, and `month_key` is recomputed from the new timestamp, then recalculate every `engagement_logs` row for that raid by recomputing `minutes_from_publish`, `scoring_window`, and `points_awarded` from the original `reacted_at` while leaving `removed_at` untouched. Keep the recalculation SQL inside `src/db/queries/update-raid-published-at.ts` or the domain service in this plan; do not modify the reaction registration files here. After the database update succeeds, call `client.chat.update` with the canonical `buildRaidMessage({ raid: updatedRaid, showPoints: false })` output so the timing row changes but the legend layout stays identical.</action>
  <verify>
    <automated>npx vitest run tests/raids/correct-raid-published-at.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/raids/correct-raid-published-at.ts` contains the exact string `authoritative_publish_time`.
    - `src/domain/raids/correct-raid-published-at.ts` contains `client.chat.update(` and `buildRaidMessage(`.
    - `src/db/queries/insert-raid-timing-correction.ts` writes the exact fields `previous_published_at`, `new_published_at`, `previous_timing_confidence`, `new_timing_confidence`, `corrected_by`, and `reason`.
    - `tests/raids/correct-raid-published-at.test.ts` covers recalculation plus correction-row creation.
    - `npx vitest run tests/raids/correct-raid-published-at.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Phase 1 can correct a low-confidence raid to an authoritative publish time, preserve a durable correction history, recompute affected scores deterministically, and refresh the Slack timing row without changing the rest of the message layout.</done>
</task>

<task type="auto">
  <name>Task 2: Expose the timing-correction workflow through a CLI entry point</name>
  <files>package.json, src/scripts/correct-raid-published-at.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, package.json, src/scripts/correct-raid-published-at.ts, src/domain/raids/correct-raid-published-at.ts</read_first>
  <action>Add the `correct:raid-time` package script as `tsx src/scripts/correct-raid-published-at.ts`. Create the executable script `src/scripts/correct-raid-published-at.ts` and wire it to `npm run correct:raid-time -- --raid-id=<uuid> --published-at=<ISO8601> --corrected-by=<slack_user_id>` so the correction path is reachable in Phase 1 instead of being a placeholder. The script must require all three flags, reject future timestamps, call `correctRaidPublishedAt`, and print a concise success or failure message for operators.</action>
  <verify>
    <automated>node -e "const pkg=require('./package.json'); process.exit(pkg.scripts['correct:raid-time'] ? 0 : 1)" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact script key `"correct:raid-time"`.
    - `src/scripts/correct-raid-published-at.ts` parses the exact flags `--raid-id`, `--published-at`, and `--corrected-by`.
    - `src/scripts/correct-raid-published-at.ts` rejects future timestamps before calling the correction service.
    - `node -e "const pkg=require('./package.json'); process.exit(pkg.scripts['correct:raid-time'] ? 0 : 1)"` exits `0`.
  </acceptance_criteria>
  <done>The D-04 correction flow is executable from the CLI with required inputs, so authoritative timing can be repaired in Phase 1 without manual code edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Timing correction CLI -> database and Slack message update | A manual correction can rewrite score interpretation if not logged carefully |
| Correction service -> audit tables | Previous and new timing facts cross into durable history here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T/R | `src/domain/raids/correct-raid-published-at.ts` | mitigate | Insert a `raid_timing_corrections` record before updating the raid, preserve prior timestamps/confidence values, and recompute engagement facts from stored `reacted_at` values. |
| T-05-02 | T | `src/db/queries/update-raid-published-at.ts` | mitigate | Recalculate `minutes_from_publish`, `scoring_window`, and `points_awarded` from persisted event times while leaving `removed_at` untouched. |
| T-05-03 | E | `src/scripts/correct-raid-published-at.ts` | mitigate | Require `--raid-id`, `--published-at`, and `--corrected-by`, and reject future timestamps before the correction service runs. |
</threat_model>

<verification>
Run `npx vitest run tests/raids/correct-raid-published-at.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
Phase 1 can repair low-confidence raids with an authoritative publish timestamp, keep an audit trail of the correction, and refresh the Slack timing row through a reachable CLI workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-07-SUMMARY.md`
</output>
