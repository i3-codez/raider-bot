---
phase: 01-slack-core-trusted-scoring
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - slack/app-manifest.yml
  - src/app/server.ts
  - src/app/slack.ts
  - src/slack/register-commands.ts
  - src/slack/register-events.ts
autonomous: true
requirements: []
user_setup:
  - service: slack
    why: "Install the Slack app, run /raid, and receive signed reaction events."
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App -> OAuth & Permissions -> Bot User OAuth Token"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App -> Basic Information -> App Credentials"
      - name: SLACK_RAID_CHANNEL_ID
        source: "Slack workspace -> target raid channel ID"
      - name: SLACK_RAID_OPERATOR_USER_IDS
        source: "Slack workspace -> comma-separated operator user IDs"
    dashboard_config:
      - task: "Install the manifest and point slash commands, event subscriptions, and interactivity to the shared Bolt HTTP endpoint (for example `/slack/events` on the deployed base URL)"
        location: "Slack App Manifest or Slack App dashboard"
must_haves:
  truths:
    - "The runtime boots a Bolt HTTP app from the validated env surface established in Plan 01."
    - "The Slack app manifest is versioned with the minimum Phase 1 scopes, slash command, and reaction event subscriptions."
    - "The manifest defines one complete HTTP delivery path for slash commands, event subscriptions, and interactivity into the shared Bolt endpoint."
    - "Later plans have explicit `registerCommands(app)` and `registerEvents(app)` extension points instead of self-registering handlers."
    - "The process started by `npm start` reaches the Bolt bootstrap through an explicit `src/app/server.ts -> src/app/slack.ts` handoff."
  artifacts:
    - path: slack/app-manifest.yml
      provides: "Versioned Slack app manifest for the Phase 1 command and event surface"
      contains: "request_url"
    - path: src/app/server.ts
      provides: "Runtime entry point that boots the Bolt app"
      contains: "createSlackApp"
    - path: src/app/slack.ts
      provides: "Bolt app bootstrap with shared registration entry points"
      contains: "registerCommands(app)"
    - path: src/slack/register-commands.ts
      provides: "Explicit command bootstrap shell"
      contains: "export function registerCommands"
    - path: src/slack/register-events.ts
      provides: "Explicit event bootstrap shell"
      contains: "export function registerEvents"
  key_links:
    - from: slack/app-manifest.yml
      to: src/app/slack.ts
      via: "shared `/slack/events` HTTPReceiver delivery path"
      pattern: "/slack/events|HTTPReceiver"
    - from: src/app/server.ts
      to: src/app/slack.ts
      via: "runtime entry point boots the Bolt app"
      pattern: "createSlackApp\\(|start\\("
    - from: src/app/slack.ts
      to: src/slack/register-commands.ts
      via: "registerCommands(app)"
      pattern: "registerCommands\\(app\\)"
    - from: src/app/slack.ts
      to: src/slack/register-events.ts
      via: "registerEvents(app)"
      pattern: "registerEvents\\(app\\)"
---

<objective>
Add the Slack-specific runtime bootstrap: the versioned app manifest, the Bolt app wrapper, and empty registration shells for commands and events.

Purpose: Later plans need a stable Slack startup surface and manifest-owned scopes before they can wire `/raid`, reactions, or message posting behavior.
Output: `slack/app-manifest.yml`, `src/app/slack.ts`, and the empty registration shells later plans extend.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the versioned Slack app manifest for the Phase 1 surface</name>
  <files>slack/app-manifest.yml</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, slack/app-manifest.yml</read_first>
  <action>Add `slack/app-manifest.yml` with the bot scopes `commands`, `chat:write`, and `reactions:read`, one slash command `/raid`, and bot event subscriptions `reaction_added` and `reaction_removed`. Because this phase uses Bolt `HTTPReceiver`, define one complete delivery path to the shared Bolt endpoint in the manifest: `features.slash_commands[].description`, `features.slash_commands[].url`, `settings.event_subscriptions.request_url`, and `settings.interactivity.is_enabled` plus `settings.interactivity.request_url`, all targeting the same deployed endpoint such as `/slack/events`. Keep the manifest limited to the Phase 1 MVP surface; do not add webhook, report, or leaderboard scopes here.</action>
  <verify>
    <automated>rg -n \"description:|url:|request_url:|interactivity:|reaction_added|reaction_removed\" slack/app-manifest.yml && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `slack/app-manifest.yml` contains `/raid`, `reaction_added`, `reaction_removed`, `commands`, `chat:write`, and `reactions:read`.
    - `slack/app-manifest.yml` contains `features.slash_commands[].description`, `features.slash_commands[].url`, `settings.event_subscriptions.request_url`, and `settings.interactivity.request_url`, all pointing to the shared Bolt endpoint such as `/slack/events`.
    - The manifest does not add out-of-scope Phase 2 or Phase 3 Slack scopes.
    - `rg -n "description:|url:|request_url:|interactivity:" slack/app-manifest.yml` exits `0`.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <done>The repo carries a versioned Slack manifest that matches the exact Phase 1 command and event surface and defines a complete HTTP delivery path into the Bolt receiver.</done>
</task>

<task type="auto">
  <name>Task 2: Create the Bolt app bootstrap, wire it into `server.ts`, and add empty registration shells</name>
  <files>src/app/server.ts, src/app/slack.ts, src/slack/register-commands.ts, src/slack/register-events.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, src/app/server.ts, src/config/env.ts, src/app/slack.ts, src/slack/register-commands.ts, src/slack/register-events.ts</read_first>
  <action>In `src/app/slack.ts`, construct a Bolt `App` backed by `HTTPReceiver`, rely on Bolt request verification with `SLACK_SIGNING_SECRET`, and call extension points `registerCommands(app)` and `registerEvents(app)`. Export a bootstrap helper such as `createSlackApp()` or equivalent from `src/app/slack.ts`. In `src/app/server.ts`, import that Bolt bootstrap and start the HTTP process from the single runtime entry point used by `npm start`, so the live process actually reaches the later `/raid` and reaction registrations. In `src/slack/register-commands.ts` and `src/slack/register-events.ts`, export empty bootstrap shells only; do not register `/raid`, modal submissions, or reaction handlers in this plan because later plans own that wiring explicitly.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/server.ts` imports the Bolt bootstrap from `src/app/slack.ts` and starts the runtime from that entry point.
    - `src/app/slack.ts` contains `registerCommands(app)` and `registerEvents(app)`.
    - `src/slack/register-commands.ts` and `src/slack/register-events.ts` export bootstrap functions but do not yet contain feature-specific listener registrations.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <done>The runtime entry point reaches the Bolt bootstrap, and the app still exposes explicit command and event seams that later plans can wire without self-registering on import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack -> Bolt HTTP receiver | Signed Slack requests cross into the runtime here |
| Repo manifest -> Slack app config | Incorrect scopes or subscriptions would alter the production app surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | `src/app/slack.ts` | mitigate | Use Bolt `HTTPReceiver` with `SLACK_SIGNING_SECRET`; do not add custom bypass middleware for Slack routes. |
| T-02-02 | E | `slack/app-manifest.yml` | mitigate | Keep the manifest versioned and limited to `/raid`, `reaction_added`, `reaction_removed`, and the scopes `commands`, `chat:write`, and `reactions:read`. |
| T-02-03 | D | `src/slack/register-commands.ts` and `src/slack/register-events.ts` | mitigate | Expose one explicit registration entry point per surface so later listeners cannot be omitted silently from app startup. |
</threat_model>

<verification>
Run `npm run typecheck`.
</verification>

<success_criteria>
The repo contains the versioned Slack manifest and the Bolt bootstrap seams Phase 1 needs before any feature-specific handlers are added.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-02-SUMMARY.md`
</output>
