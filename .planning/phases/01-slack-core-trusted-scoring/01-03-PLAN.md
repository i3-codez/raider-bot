---
phase: 01-slack-core-trusted-scoring
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/slack/blocks/build-raid-message.ts
  - src/domain/raids/types.ts
  - src/domain/scoring/types.ts
  - src/domain/scoring/action-registry.ts
  - src/domain/scoring/scoring-config.ts
  - tests/domain/scoring-config.test.ts
  - tests/slack/build-raid-message.test.ts
autonomous: true
requirements:
  - RAID-06
  - ENG-01
  - ENG-04
must_haves:
  truths:
    - "The raid legend, timing windows, and Slack message layout come from one canonical source."
    - "The resolved Phase 1 timing matrix is the exact `10/8/6/3/0` contract across the five windows."
    - "Downstream plans can import stable raid and scoring types instead of redefining them."
  artifacts:
    - path: src/domain/raids/types.ts
      provides: "Canonical raid domain types for Phase 1"
      contains: "RaidTimingConfidence"
    - path: src/domain/scoring/action-registry.ts
      provides: "Canonical emoji-to-action registry"
      contains: "speech_balloon"
    - path: src/domain/scoring/scoring-config.ts
      provides: "Fixed five-window scoring matrix"
      contains: "60m+"
    - path: src/slack/blocks/build-raid-message.ts
      provides: "Canonical Block Kit builder shared by posting and correction flows"
      exports:
        - buildRaidMessage
  key_links:
    - from: src/slack/blocks/build-raid-message.ts
      to: src/domain/scoring/action-registry.ts
      via: "shared action metadata import"
      pattern: "ACTION_REGISTRY"
    - from: src/slack/blocks/build-raid-message.ts
      to: src/domain/scoring/scoring-config.ts
      via: "shared timing window import"
      pattern: "SCORING_WINDOWS"
---

<objective>
Define the shared Phase 1 scoring and raid-message contracts: canonical raid and scoring types, the centralized emoji registry, the resolved timing matrix, and the Slack message builder consumed by posting and correction flows.

Purpose: Manual raid creation, reaction scoring, and timing correction all depend on one source of truth for action labels, timing windows, and message copy.
Output: Shared type files, scoring config, canonical Block Kit builder, and tests that lock the matrix and message in place.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared raid and scoring contracts</name>
  <files>src/domain/raids/types.ts, src/domain/scoring/types.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, tests/domain/scoring-config.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, .planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md, src/lib/time.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts</read_first>
  <behavior>
    - Test 1: the scoring config exports exactly five windows with labels `0-10m`, `10-20m`, `20-30m`, `30-60m`, and `60m+`, and point values `10`, `8`, `6`, `3`, and `0`.
    - Test 2: the action registry exposes exactly four canonical Slack emoji mappings: `heart -> like`, `speech_balloon -> comment`, `repeat -> repost`, and `memo -> quote_post`.
    - Test 3: the exported types cover `Platform = "x"`, `RaidTimingConfidence = "high" | "low"`, `ActionType = "like" | "comment" | "repost" | "quote_post"`, and the scoring-window union used by downstream services.
  </behavior>
  <action>Create shared type files for `Platform = "x"`, `RaidTimingConfidence = "high" | "low"`, `ActionType = "like" | "comment" | "repost" | "quote_post"`, and the scoring-window union. In `src/domain/scoring/action-registry.ts`, export the exact UI-SPEC legend entries `:heart: Like`, `:speech_balloon: Comment`, `:repeat: Repost`, and `:memo: Quote post`. In `src/domain/scoring/scoring-config.ts`, export the fixed timing matrix `0-10m => 10`, `10-20m => 8`, `20-30m => 6`, `30-60m => 3`, `60m+ => 0`. Add `tests/domain/scoring-config.test.ts` to lock the canonical scoring windows and emoji registry in place. Do not introduce any Slack handlers or database code in this task.</action>
  <verify>
    <automated>npx vitest run tests/domain/scoring-config.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/scoring/action-registry.ts` contains the exact emoji keys `heart`, `speech_balloon`, `repeat`, and `memo`.
    - `src/domain/scoring/scoring-config.ts` contains the exact labels `0-10m`, `10-20m`, `20-30m`, `30-60m`, and `60m+`.
    - `tests/domain/scoring-config.test.ts` asserts the resolved `10/8/6/3/0` matrix.
    - `npx vitest run tests/domain/scoring-config.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Downstream plans can import stable types, action mappings, and the resolved timing matrix without recreating or guessing any Phase 1 scoring rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the canonical raid-message builder from the UI contract</name>
  <files>src/slack/blocks/build-raid-message.ts, tests/slack/build-raid-message.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, .planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md, src/domain/raids/types.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, src/slack/blocks/build-raid-message.ts</read_first>
  <behavior>
    - Test 1: `buildRaidMessage` renders the exact low-confidence warning `:warning: Published time not provided. Using Slack post time, so timing-based scores are approximate.`
    - Test 2: `buildRaidMessage` renders the exact footer note `One score per person per action type. Remove your reaction to undo that action.`
    - Test 3: the message builder renders the canonical header, standalone URL row, dedicated timing row, four-item legend, and five-window timing section from shared config instead of duplicated strings.
  </behavior>
  <action>In `src/slack/blocks/build-raid-message.ts`, implement the full UI-SPEC message contract now: top-level `text` fallback, `Raid live: {client_name} on X` header, standalone URL row, dedicated timing row, divider, exact four-item legend, explicit five-window timing section, and exact footer note; use the same action registry and timing config, and include the D-03 low-confidence warning row verbatim. Add `tests/slack/build-raid-message.test.ts` to lock the canonical message copy and layout contract in place. Do not change the runtime bootstrap or database layer in this task.</action>
  <verify>
    <automated>npx vitest run tests/slack/build-raid-message.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/slack/blocks/build-raid-message.ts` contains the exact warning string `Using Slack post time, so timing-based scores are approximate.` and the exact footer string `Remove your reaction to undo that action.`
    - `src/slack/blocks/build-raid-message.ts` imports the shared action registry and scoring window config rather than duplicating legend or timing data.
    - `tests/slack/build-raid-message.test.ts` asserts the low-confidence warning and footer note directly.
    - `npx vitest run tests/slack/build-raid-message.test.ts` exits `0`.
  </acceptance_criteria>
  <done>The shared Slack message contract is implemented from one canonical registry and timing matrix, so posting and correction flows can reuse it without copy drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared scoring contracts -> runtime handlers | Incorrect shared config would distort every score or visible instruction |
| Message builder -> Slack surfaces | Staff trust depends on the visible legend and timing guidance matching runtime behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | `src/domain/scoring/scoring-config.ts` | mitigate | Lock the resolved `10/8/6/3/0` matrix in code and tests so timing windows cannot drift silently. |
| T-03-02 | T | `src/domain/scoring/action-registry.ts` | mitigate | Keep one canonical emoji registry for Slack copy, scoring services, and tests. |
| T-03-03 | R | `src/slack/blocks/build-raid-message.ts` | mitigate | Build message legend and timing sections from shared config so the staff-facing instructions always match the scoring engine. |
</threat_model>

<verification>
Run `npx vitest run tests/domain/scoring-config.test.ts tests/slack/build-raid-message.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
The repo contains a single source of truth for Phase 1 raid copy, emoji semantics, and timing windows, and later plans can consume those contracts without recreating them.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-03-SUMMARY.md`
</output>
