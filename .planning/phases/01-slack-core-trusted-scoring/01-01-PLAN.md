---
phase: 01-slack-core-trusted-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - .env.example
  - src/config/env.ts
  - src/lib/logger.ts
  - src/lib/time.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "The repo establishes a typed Node 24 TypeScript runtime foundation from validated environment variables."
    - "Logging redacts sensitive connection and Slack secrets before any downstream plan emits structured logs."
    - "Shared time helpers exist for Eastern display, month-key derivation, and score-window math."
  artifacts:
    - path: package.json
      provides: "Runtime scripts and dependencies for the service"
      contains: "\"typecheck\""
    - path: src/config/env.ts
      provides: "Centralized environment validation for Slack and Postgres settings"
      contains: "SLACK_RAID_OPERATOR_USER_IDS"
    - path: src/lib/logger.ts
      provides: "Structured logger with secret redaction"
      contains: "redact"
    - path: src/lib/time.ts
      provides: "Shared Eastern-time and month-key helpers"
      contains: "deriveMonthKey"
---

<objective>
Establish the Phase 1 repo and runtime foundation: package tooling, environment validation, logging redaction, and shared time utilities.

Purpose: Every downstream plan depends on a stable TypeScript service base before Slack bootstrap, schema work, or scoring logic can be added safely.
Output: Repo scripts, validated env loading, the shared logger, and reusable time helpers.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the Node 24 TypeScript repo tooling</name>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, .gitignore, .env.example</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, package.json, tsconfig.json</read_first>
  <action>Initialize a Node 24 TypeScript service with `@slack/bolt`, `postgres`, `zod`, and `pino`, plus dev dependencies `typescript`, `tsx`, `vitest`, and `@types/node`. In `package.json`, add scripts `dev: "tsx watch src/app/server.ts"`, `start: "tsx src/app/server.ts"`, `test: "vitest run"`, and `typecheck: "tsc --noEmit"`. Configure `tsconfig.json`, `vitest.config.ts`, `.gitignore`, and `.env.example` for a backend-only Slack bot service so the later Slack bootstrap plan can plug into the agreed runtime conventions.</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact script keys `"dev"`, `"start"`, `"test"`, and `"typecheck"`.
    - `.gitignore` excludes `node_modules`, `.env*`, `.supabase`, and OS junk.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <done>The repo has the baseline runtime scripts and dependency graph later plans can build on without revisiting core tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Add validated environment loading, the redacted logger, and shared time helpers</name>
  <files>src/config/env.ts, src/lib/logger.ts, src/lib/time.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, src/config/env.ts, src/lib/logger.ts, src/lib/time.ts</read_first>
  <action>In `src/config/env.ts`, validate `DATABASE_URL`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_RAID_CHANNEL_ID`, and `SLACK_RAID_OPERATOR_USER_IDS`, plus optional `SLACK_APP_TOKEN`, `LOG_LEVEL` defaulting to `info`, and `APP_PORT` defaulting to `3000`. In `src/lib/logger.ts`, configure `pino` redaction for `DATABASE_URL`, `SLACK_BOT_TOKEN`, and `SLACK_SIGNING_SECRET`. In `src/lib/time.ts`, export `toEasternLabel(date)`, `deriveMonthKey(date)`, and `minutesBetween(later, earlier)` so later plans share one timing vocabulary.</action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/env.ts` validates `DATABASE_URL`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_RAID_CHANNEL_ID`, and `SLACK_RAID_OPERATOR_USER_IDS`.
    - `src/lib/logger.ts` redacts `DATABASE_URL`, `SLACK_BOT_TOKEN`, and `SLACK_SIGNING_SECRET`.
    - `src/lib/time.ts` exports `toEasternLabel`, `deriveMonthKey`, and `minutesBetween`.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <done>The runtime has validated config, safe structured logging, and reusable time helpers ready before any Slack or DB behavior is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Environment -> runtime | Secrets and config enter the process here |
| Runtime -> logs | Secrets could leak if structured logs are not redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I | `src/config/env.ts` | mitigate | Validate all required Slack and Postgres settings at startup so the service fails fast instead of running partially configured. |
| T-01-02 | I | `src/lib/logger.ts` | mitigate | Redact `DATABASE_URL`, `SLACK_BOT_TOKEN`, and `SLACK_SIGNING_SECRET` from structured logs. |
| T-01-03 | T | `src/lib/time.ts` | mitigate | Centralize month-key and elapsed-minute helpers so later plans do not hand-roll conflicting scoring math. |
</threat_model>

<verification>
Run `npm install` and `npm run typecheck`.
</verification>

<success_criteria>
The repo has a stable TypeScript foundation, validated environment loading, safe logging defaults, and shared time helpers before any Slack or database features are layered on top.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-01-SUMMARY.md`
</output>
