---
phase: 01-slack-core-trusted-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vitest.config.ts
  - .gitignore
  - .env.example
  - slack/app-manifest.yml
  - src/app/server.ts
  - src/app/slack.ts
  - src/config/env.ts
  - src/lib/logger.ts
  - src/lib/time.ts
  - src/slack/register-commands.ts
  - src/slack/register-events.ts
  - src/slack/blocks/build-raid-message.ts
  - src/domain/raids/types.ts
  - src/domain/scoring/types.ts
  - src/domain/scoring/action-registry.ts
  - src/domain/scoring/scoring-config.ts
  - tests/domain/scoring-config.test.ts
  - tests/slack/build-raid-message.test.ts
autonomous: true
requirements:
  - ENG-01
  - ENG-04
user_setup:
  - service: slack
    why: "Install the Slack app, run /raid, and receive signed reaction events."
    env_vars:
      - name: SLACK_BOT_TOKEN
        source: "Slack App -> OAuth & Permissions -> Bot User OAuth Token"
      - name: SLACK_SIGNING_SECRET
        source: "Slack App -> Basic Information -> App Credentials"
      - name: SLACK_RAID_CHANNEL_ID
        source: "Slack workspace -> target raid channel ID"
      - name: SLACK_RAID_OPERATOR_USER_IDS
        source: "Slack workspace -> comma-separated operator user IDs"
    dashboard_config:
      - task: "Install manifest scopes, the /raid slash command, and the reaction_added/reaction_removed event subscriptions"
        location: "Slack App Manifest or Slack App dashboard"
must_haves:
  truths:
    - "The repo boots a typed Bolt HTTP app from validated environment variables."
    - "The raid legend, timing windows, and Slack message layout come from one canonical source."
    - "The app startup exposes explicit command and event bootstrap points for later feature wiring."
  artifacts:
    - path: package.json
      provides: "Runtime scripts and dependencies for the Bolt service"
      contains: "\"@slack/bolt\""
    - path: src/app/slack.ts
      provides: "Bolt app bootstrap with shared registration entry points"
      contains: "registerCommands(app)"
    - path: src/slack/blocks/build-raid-message.ts
      provides: "Canonical Block Kit builder shared by posting and correction flows"
      exports:
        - buildRaidMessage
    - path: src/domain/scoring/scoring-config.ts
      provides: "Fixed five-window scoring matrix"
      contains: "60m+"
  key_links:
    - from: src/app/slack.ts
      to: src/slack/register-commands.ts
      via: "registerCommands(app)"
      pattern: "registerCommands\\(app\\)"
    - from: src/app/slack.ts
      to: src/slack/register-events.ts
      via: "registerEvents(app)"
      pattern: "registerEvents\\(app\\)"
    - from: src/slack/blocks/build-raid-message.ts
      to: src/domain/scoring/action-registry.ts
      via: "shared action metadata import"
      pattern: "ACTION_REGISTRY"
    - from: src/slack/blocks/build-raid-message.ts
      to: src/domain/scoring/scoring-config.ts
      via: "shared timing window import"
      pattern: "SCORING_WINDOWS"
---

<objective>
Create the runnable Phase 1 runtime foundation: Bolt HTTP bootstrap, env validation, versioned Slack manifest, and the canonical scoring/message contracts consumed by later plans.

Purpose: Downstream plans need stable startup wiring, message composition contracts, and scoring windows before they can add manual raid flow, reaction scoring, or schema-backed persistence.
Output: Runtime/config files, startup registration shells, shared scoring modules, and the canonical Block Kit builder.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the Bolt runtime, env validation, and manifest tooling</name>
  <files>package.json, package-lock.json, tsconfig.json, vitest.config.ts, .gitignore, .env.example, slack/app-manifest.yml, src/app/server.ts, src/app/slack.ts, src/config/env.ts, src/lib/logger.ts, src/lib/time.ts, src/slack/register-commands.ts, src/slack/register-events.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, .planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md, package.json, tsconfig.json, src/app/server.ts, src/app/slack.ts, src/config/env.ts</read_first>
  <action>Initialize a Node 24 TypeScript service with `@slack/bolt`, `postgres`, `zod`, and `pino`, plus dev dependencies `typescript`, `tsx`, `vitest`, and `@types/node`. In `package.json`, add scripts `dev: "tsx watch src/app/server.ts"`, `start: "tsx src/app/server.ts"`, `test: "vitest run"`, and `typecheck: "tsc --noEmit"`. In `src/config/env.ts`, validate `DATABASE_URL`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_RAID_CHANNEL_ID`, and `SLACK_RAID_OPERATOR_USER_IDS`, plus optional `SLACK_APP_TOKEN`, `LOG_LEVEL` defaulting to `info`, and `APP_PORT` defaulting to `3000`. In `src/app/slack.ts`, construct a Bolt `App` backed by `HTTPReceiver`, rely on Bolt request verification with `SLACK_SIGNING_SECRET`, and call extension points `registerCommands(app)` and `registerEvents(app)`. In `src/slack/register-commands.ts` and `src/slack/register-events.ts`, export empty bootstrap shells only; do not register `/raid`, modal submissions, or reaction handlers in this plan because later plans own that wiring explicitly. In `src/lib/logger.ts`, configure `pino` redaction for `DATABASE_URL`, `SLACK_BOT_TOKEN`, and `SLACK_SIGNING_SECRET`. In `src/lib/time.ts`, export `toEasternLabel(date)`, `deriveMonthKey(date)`, and `minutesBetween(later, earlier)`. Add `slack/app-manifest.yml` with the bot scopes `commands`, `chat:write`, and `reactions:read`, one slash command `/raid`, and bot event subscriptions `reaction_added` and `reaction_removed`. Keep `.gitignore` strict for `node_modules`, `.env*`, `.supabase`, and OS junk.</action>
  <verify>
    <automated>npm install && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains the exact script keys `"dev"`, `"start"`, `"test"`, and `"typecheck"`.
    - `src/config/env.ts` validates `DATABASE_URL`, `SLACK_BOT_TOKEN`, `SLACK_SIGNING_SECRET`, `SLACK_RAID_CHANNEL_ID`, and `SLACK_RAID_OPERATOR_USER_IDS`.
    - `slack/app-manifest.yml` contains `/raid`, `reaction_added`, `reaction_removed`, `commands`, `chat:write`, and `reactions:read`.
    - `src/slack/register-commands.ts` and `src/slack/register-events.ts` export bootstrap functions but do not yet contain feature-specific listener registrations.
    - `npm run typecheck` exits `0`.
  </acceptance_criteria>
  <done>The repo boots from typed environment variables, exposes the shared app startup entry points later plans depend on, and includes the versioned Slack app manifest plus runtime scripts required by downstream plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define the shared scoring and raid-message contracts</name>
  <files>src/slack/blocks/build-raid-message.ts, src/domain/raids/types.ts, src/domain/scoring/types.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, tests/domain/scoring-config.test.ts, tests/slack/build-raid-message.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, .planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md, package.json, src/lib/time.ts, src/slack/blocks/build-raid-message.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts</read_first>
  <behavior>
    - Test 1: the scoring config exports exactly five windows with labels `0-10m`, `10-20m`, `20-30m`, `30-60m`, and `60m+`, and point values `10`, `8`, `6`, `3`, and `0`.
    - Test 2: the action registry exposes exactly four canonical Slack emoji mappings: `heart -> like`, `speech_balloon -> comment`, `repeat -> repost`, and `memo -> quote_post`.
    - Test 3: `buildRaidMessage` renders the exact low-confidence warning `:warning: Published time not provided. Using Slack post time, so timing-based scores are approximate.` and the exact footer note `One score per person per action type. Remove your reaction to undo that action.`
  </behavior>
  <action>Create shared type files for `Platform = "x"`, `RaidTimingConfidence = "high" | "low"`, `ActionType = "like" | "comment" | "repost" | "quote_post"`, and the scoring-window union. In `src/domain/scoring/action-registry.ts`, export the exact UI-SPEC legend entries `:heart: Like`, `:speech_balloon: Comment`, `:repeat: Repost`, and `:memo: Quote post`. In `src/domain/scoring/scoring-config.ts`, export the fixed timing matrix `0-10m => 10`, `10-20m => 8`, `20-30m => 6`, `30-60m => 3`, `60m+ => 0`. In `src/slack/blocks/build-raid-message.ts`, implement the full UI-SPEC message contract now: top-level `text` fallback, `Raid live: {client_name} on X` header, standalone URL row, dedicated timing row, divider, exact four-item legend, explicit five-window timing section, and exact footer note; use the same action registry and timing config, and include the D-03 low-confidence warning row verbatim. Add `tests/domain/scoring-config.test.ts` and `tests/slack/build-raid-message.test.ts` to lock the canonical scoring and message copy in place. Do not change the runtime bootstrap or manifest in this task; this task owns only the shared contracts later plans consume.</action>
  <verify>
    <automated>npx vitest run tests/domain/scoring-config.test.ts tests/slack/build-raid-message.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/scoring/action-registry.ts` contains the exact emoji keys `heart`, `speech_balloon`, `repeat`, and `memo`.
    - `src/domain/scoring/scoring-config.ts` contains the exact labels `0-10m`, `10-20m`, `20-30m`, `30-60m`, and `60m+`.
    - `src/slack/blocks/build-raid-message.ts` contains the exact warning string `Using Slack post time, so timing-based scores are approximate.` and the exact footer string `Remove your reaction to undo that action.`
    - `npx vitest run tests/domain/scoring-config.test.ts tests/slack/build-raid-message.test.ts` exits `0`.
  </acceptance_criteria>
  <done>The shared Slack message contract is implemented from one canonical registry and timing matrix, and downstream plans can import stable types, emoji mappings, and scoring windows without recreating them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack -> Bolt HTTP receiver | Untrusted signed requests cross into the runtime here |
| Runtime -> logs | Secrets and database URLs could leak if logging is not redacted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | `src/app/slack.ts` | mitigate | Use Bolt `HTTPReceiver` with `SLACK_SIGNING_SECRET`; do not add custom bypass middleware for Slack routes. |
| T-01-02 | I | `src/config/env.ts` and `src/lib/logger.ts` | mitigate | Validate secrets at startup and redact `DATABASE_URL`, `SLACK_BOT_TOKEN`, and `SLACK_SIGNING_SECRET` from all structured logs. |
| T-01-03 | E | `slack/app-manifest.yml` | mitigate | Keep the manifest versioned and limited to `/raid`, `reaction_added`, `reaction_removed`, and the scopes `commands`, `chat:write`, and `reactions:read`. |
</threat_model>

<verification>
Run `npm install`, `npx vitest run tests/domain/scoring-config.test.ts tests/slack/build-raid-message.test.ts`, and `npm run typecheck`.
</verification>

<success_criteria>
The repo contains a runnable Bolt foundation, a single source of truth for the raid legend and timing rules, and explicit bootstrap files that later plans can wire without changing the shared contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-01-SUMMARY.md`
</output>
