---
phase: 01-slack-core-trusted-scoring
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/db/sql.ts
  - supabase/migrations/20260410213000_phase1_core.sql
  - tests/db/phase1-schema.test.ts
autonomous: true
requirements:
  - RAID-03
  - ENG-06
user_setup:
  - service: supabase
    why: "Persist raid posts, engagement logs, and timing correction history."
    env_vars:
      - name: DATABASE_URL
        source: "Supabase Project -> Connect -> Transaction pooler or direct Postgres URL"
    dashboard_config:
      - task: "Link the local repo or authenticate the CLI before running schema push"
        location: "Supabase CLI / Supabase Project Settings"
must_haves:
  truths:
    - "The database schema can store raid posts, engagement logs, and timing-correction audit history."
    - "The query layer can open a pooled Postgres connection from `DATABASE_URL`."
    - "The committed schema is statically verifiable before it is pushed to Supabase."
  artifacts:
    - path: src/db/sql.ts
      provides: "Shared Postgres connection and query entry point"
      contains: "DATABASE_URL"
    - path: supabase/migrations/20260410213000_phase1_core.sql
      provides: "Initial Phase 1 schema"
      contains: "CREATE TABLE raid_posts"
    - path: tests/db/phase1-schema.test.ts
      provides: "Static schema assertions for required tables and indexes"
      contains: "engagement_logs_one_action_per_user_post"
  key_links:
    - from: supabase/migrations/20260410213000_phase1_core.sql
      to: src/db/sql.ts
      via: "query layer matches pushed schema"
      pattern: "raid_posts|engagement_logs|raid_timing_corrections"
    - from: tests/db/phase1-schema.test.ts
      to: supabase/migrations/20260410213000_phase1_core.sql
      via: "static schema assertions"
      pattern: "20260410213000_phase1_core.sql"
---

<objective>
Create the Phase 1 persistence foundation: the shared Postgres helper, the initial Supabase migration, and the schema verification needed before runtime features write real data.

Purpose: Manual raid intake, reaction scoring, and timing corrections all depend on a concrete schema that matches the audit model from RAID-03, ENG-06, and D-04.
Output: `src/db/sql.ts`, the initial migration for raid and engagement data, and a test that proves the migration contains the required tables, columns, and uniqueness guarantees.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and push the Phase 1 Supabase schema</name>
  <files>src/db/sql.ts, supabase/migrations/20260410213000_phase1_core.sql, tests/db/phase1-schema.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, src/db/sql.ts, supabase/migrations/20260410213000_phase1_core.sql</read_first>
  <behavior>
    - Test 1: the migration defines `raid_posts`, `engagement_logs`, and `raid_timing_corrections`.
    - Test 2: `raid_posts` stores `published_at`, `slack_posted_at`, `slack_message_ts`, `slack_channel_id`, `timing_confidence`, and `month_key`.
    - Test 3: `engagement_logs` stores `reacted_at`, `minutes_from_publish`, `scoring_window`, `points_awarded`, and nullable `removed_at`, with unique index `engagement_logs_one_action_per_user_post`.
    - Test 4: `raid_timing_corrections` stores previous/new publish time and timing-confidence values so D-04 has durable audit history.
  </behavior>
  <action>Create `src/db/sql.ts` using the `postgres` driver and typed helper functions for opening a pooled connection from `DATABASE_URL`. Add `supabase/migrations/20260410213000_phase1_core.sql` with three tables: `raid_posts`, `engagement_logs`, and `raid_timing_corrections`. `raid_posts` must include `id`, `post_url`, `client_name`, `platform`, `created_by_slack_user_id`, nullable `published_at`, non-null `slack_posted_at`, non-null `slack_message_ts`, non-null `slack_channel_id`, non-null `timing_confidence`, non-null `month_key`, `created_at`, and `updated_at`, plus a unique constraint on `(slack_channel_id, slack_message_ts)`. `engagement_logs` must include `id`, `raid_post_id`, `slack_user_id`, `slack_reaction`, `action_type`, `reacted_at`, `minutes_from_publish`, `scoring_window`, `points_awarded`, nullable `removed_at`, `created_at`, and `updated_at`, plus the unique index `engagement_logs_one_action_per_user_post` on `(raid_post_id, slack_user_id, action_type)`. `raid_timing_corrections` must include `id`, `raid_post_id`, `previous_published_at`, `new_published_at`, `previous_timing_confidence`, `new_timing_confidence`, `corrected_by`, `reason`, and `corrected_at` so D-04 has durable correction history. Add a static schema test that asserts those exact tables, key columns, and the unique index are present in the migration file. After the migration exists, run the blocking schema push command `npx supabase db push --linked` so the linked database matches the committed SQL before any downstream verification continues.</action>
  <verify>
    <automated>npx vitest run tests/db/phase1-schema.test.ts && npm run typecheck && npx supabase db push --linked</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260410213000_phase1_core.sql` contains `CREATE TABLE raid_posts`, `CREATE TABLE engagement_logs`, and `CREATE TABLE raid_timing_corrections`.
    - `supabase/migrations/20260410213000_phase1_core.sql` contains the exact column names `published_at`, `slack_posted_at`, `slack_message_ts`, `slack_channel_id`, `timing_confidence`, `month_key`, `minutes_from_publish`, `scoring_window`, `points_awarded`, and `removed_at`.
    - `supabase/migrations/20260410213000_phase1_core.sql` contains the exact index name `engagement_logs_one_action_per_user_post`.
    - `tests/db/phase1-schema.test.ts` asserts the presence of `raid_timing_corrections` and `engagement_logs_one_action_per_user_post`.
    - `npx vitest run tests/db/phase1-schema.test.ts` exits `0`.
    - `npx supabase db push --linked` completes without schema errors.
  </acceptance_criteria>
  <done>The Phase 1 schema is committed, locally verifiable, and applied to the linked Supabase project so downstream plans can persist real raid and engagement data immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime -> Postgres | Canonical raid, engagement, and correction facts are persisted here |
| Local migration -> linked Supabase project | Schema changes cross into the real managed database here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `supabase/migrations/20260410213000_phase1_core.sql` | mitigate | Lock ENG-02 into the schema with `engagement_logs_one_action_per_user_post` and keep Phase 1 tables explicit in committed SQL. |
| T-02-02 | R | `raid_timing_corrections` table | mitigate | Persist previous/new publish time and timing-confidence values so D-04 corrections remain explainable later. |
| T-02-03 | D | `src/db/sql.ts` | mitigate | Use one pooled `postgres` connection entry point from validated `DATABASE_URL` instead of ad hoc clients in each handler. |
</threat_model>

<verification>
Run `npx vitest run tests/db/phase1-schema.test.ts`, `npm run typecheck`, and `npx supabase db push --linked`.
</verification>

<success_criteria>
The repo contains a concrete Phase 1 schema and DB helper that can represent raid posts, engagement logs, and timing corrections without later schema guesswork, and the linked Supabase project is updated from committed SQL.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-04-SUMMARY.md`
</output>
