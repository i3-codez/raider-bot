---
phase: 01-slack-core-trusted-scoring
plan: 04
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/slack/register-events.ts
  - src/slack/events/register-reaction-handlers.ts
  - src/domain/scoring/claim-engagement.ts
  - src/domain/scoring/reverse-engagement.ts
  - src/db/queries/find-raid-by-slack-ref.ts
  - src/db/queries/engagement-logs.ts
  - tests/scoring/engagement-flow.test.ts
autonomous: true
requirements:
  - ENG-04
  - ENG-02
  - ENG-03
  - ENG-05
  - ENG-06
must_haves:
  truths:
    - "Supported reactions create at most one active score per user/action/post while different action types still stack."
    - "Removing a supported reaction deactivates the score without deleting the engagement row."
    - "Reaction listeners are reachable from app startup and silently ignore unsupported or non-raid reactions."
  artifacts:
    - path: src/slack/register-events.ts
      provides: "Bootstrap wiring for Phase 1 Slack event handlers"
      contains: "registerReactionHandlers(app)"
    - path: src/slack/events/register-reaction-handlers.ts
      provides: "Reaction-added and reaction-removed listener registration"
      contains: "reaction_added"
    - path: src/db/queries/engagement-logs.ts
      provides: "Canonical engagement claim and reversal SQL"
      contains: "ON CONFLICT (raid_post_id, slack_user_id, action_type)"
  key_links:
    - from: src/slack/register-events.ts
      to: src/slack/events/register-reaction-handlers.ts
      via: "bootstrap registration"
      pattern: "registerReactionHandlers\\(app\\)"
    - from: src/slack/events/register-reaction-handlers.ts
      to: src/db/queries/find-raid-by-slack-ref.ts
      via: "message lookup before scoring"
      pattern: "findRaidBySlackRef\\("
    - from: src/domain/scoring/claim-engagement.ts
      to: src/db/queries/engagement-logs.ts
      via: "claim persistence"
      pattern: "claimEngagementLog\\("
---

<objective>
Implement deterministic reaction scoring, reversible audit logging, and explicit runtime wiring from the Slack app bootstrap into the reaction handlers.

Purpose: The Phase 1 raid loop is only trustworthy if duplicate reactions cannot inflate points, removals preserve auditability, and the reaction listeners are actually reachable from `registerEvents(app)`.
Output: Event bootstrap wiring, reaction listeners, scoring/reversal services, engagement query layer, and tests for stacking, dedupe, and removal.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-UI-SPEC.md
@AGENTS.md

<interfaces>
Expected shared contracts from Plans 01-02:

From `src/domain/scoring/scoring-config.ts`:
```typescript
export const SCORING_WINDOWS: Array<{
  label: "0-10m" | "10-20m" | "20-30m" | "30-60m" | "60m+";
  minMinutes: number;
  maxMinutes: number | null;
  points: 10 | 8 | 6 | 3 | 0;
}>;
```

From `src/domain/scoring/action-registry.ts`:
```typescript
export const ACTION_REGISTRY: Record<string, {
  actionType: "like" | "comment" | "repost" | "quote_post";
  label: string;
  emoji: string;
}>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire Slack event bootstrap into the reaction handlers</name>
  <files>src/slack/register-events.ts, src/slack/events/register-reaction-handlers.ts, src/db/queries/find-raid-by-slack-ref.ts, tests/scoring/engagement-flow.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, src/config/env.ts, src/lib/time.ts, src/domain/raids/types.ts, src/domain/scoring/types.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, src/slack/register-events.ts, src/slack/events/register-reaction-handlers.ts, src/db/sql.ts, supabase/migrations/20260410213000_phase1_core.sql</read_first>
  <behavior>
    - Test 1: `heart`, `speech_balloon`, `repeat`, and `memo` map to `like`, `comment`, `repost`, and `quote_post`, and unsupported emoji are ignored silently.
    - Test 2: `registerEvents(app)` calls the exported reaction-registration function so both `reaction_added` and `reaction_removed` listeners are bootstrapped from the app startup path.
    - Test 3: non-message items, unknown raid message references, and unsupported emoji are ignored silently before scoring.
  </behavior>
  <action>In `src/slack/events/register-reaction-handlers.ts`, export `registerReactionHandlers(app)` and register `app.event("reaction_added", ...)` and `app.event("reaction_removed", ...)` inside that function; do not self-register on import. In `src/slack/register-events.ts`, import `registerReactionHandlers` and call it inside `registerEvents(app)` so the Phase 1 runtime bootstrap reaches the reaction flow. Ignore anything where `event.item.type !== "message"`, where the message lookup does not resolve to a known raid, or where the emoji is not one of the canonical four. In `src/db/queries/find-raid-by-slack-ref.ts`, look up raids by the exact `(slack_channel_id, slack_message_ts)` pair. Extend `tests/scoring/engagement-flow.test.ts` so the bootstrap path and silent-ignore behavior are asserted directly, not inferred.</action>
  <verify>
    <automated>npx vitest run tests/scoring/engagement-flow.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/slack/register-events.ts` contains `registerReactionHandlers(app)`.
    - `src/slack/events/register-reaction-handlers.ts` contains `app.event("reaction_added"` and `app.event("reaction_removed"`.
    - `src/db/queries/find-raid-by-slack-ref.ts` looks up raids by `slack_channel_id` and `slack_message_ts`.
    - `tests/scoring/engagement-flow.test.ts` includes a bootstrap assertion through `registerEvents(app)`.
    - `npx vitest run tests/scoring/engagement-flow.test.ts` exits `0`.
  </acceptance_criteria>
  <done>The reaction listeners are reachable from the app bootstrap, use exact message references for raid lookup, and silently ignore irrelevant or unsupported reactions before any scoring work happens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement reaction scoring, stacking, dedupe, and reversal persistence</name>
  <files>src/domain/scoring/claim-engagement.ts, src/domain/scoring/reverse-engagement.ts, src/db/queries/engagement-logs.ts, tests/scoring/engagement-flow.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md, .planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md, src/lib/time.ts, src/domain/raids/types.ts, src/domain/scoring/types.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, src/domain/scoring/claim-engagement.ts, src/domain/scoring/reverse-engagement.ts, src/db/queries/engagement-logs.ts, supabase/migrations/20260410213000_phase1_core.sql</read_first>
  <behavior>
    - Test 1: duplicate `reaction_added` delivery for the same raid, user, and action type does not create a second active score.
    - Test 2: different action types for the same user and raid stack as separate active engagement rows.
    - Test 3: `reaction_removed` sets `removed_at` and leaves the engagement row present for audit.
    - Test 4: re-adding a removed action reactivates the same canonical row instead of creating a duplicate.
  </behavior>
  <action>In `src/domain/scoring/claim-engagement.ts`, compute the scoring timestamp from `raid.publishedAt ?? raid.slackPostedAt`, calculate `minutes_from_publish`, choose the scoring window from the exact Plan 01 matrix `10/8/6/3/0`, and persist one canonical row per `(raid_post_id, slack_user_id, action_type)`. In `src/db/queries/engagement-logs.ts`, implement `claimEngagementLog` using the exact SQL contract `ON CONFLICT (raid_post_id, slack_user_id, action_type) DO UPDATE SET slack_reaction = EXCLUDED.slack_reaction, reacted_at = EXCLUDED.reacted_at, minutes_from_publish = EXCLUDED.minutes_from_publish, scoring_window = EXCLUDED.scoring_window, points_awarded = EXCLUDED.points_awarded, removed_at = NULL, updated_at = NOW() WHERE engagement_logs.removed_at IS NOT NULL;` so duplicate active deliveries do nothing, but re-adding after removal reactivates the same row. In `src/domain/scoring/reverse-engagement.ts`, mark the row inactive with `removed_at = eventTime`; never delete it. This task implements ENG-02, ENG-03, ENG-05, and ENG-06 directly.</action>
  <verify>
    <automated>npx vitest run tests/scoring/engagement-flow.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/queries/engagement-logs.ts` contains the exact clause `ON CONFLICT (raid_post_id, slack_user_id, action_type)`.
    - `src/db/queries/engagement-logs.ts` contains the exact clause `WHERE engagement_logs.removed_at IS NOT NULL`.
    - `src/domain/scoring/reverse-engagement.ts` updates `removed_at` and does not contain a `DELETE FROM engagement_logs` statement.
    - `tests/scoring/engagement-flow.test.ts` includes cases for duplicate delivery, action stacking, and reaction removal.
    - `npx vitest run tests/scoring/engagement-flow.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Reaction events score exactly once per user/action/post, different actions still stack, unsupported reactions stay silent, and removals deactivate scores without damaging the engagement audit row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slack reaction events -> scoring service | Untrusted event delivery can be duplicated, reordered, or malformed |
| Scoring service -> audit tables | Canonical points and audit evidence are written here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/db/queries/engagement-logs.ts` | mitigate | Enforce one-row-per-action with the unique index and the exact `ON CONFLICT ... WHERE engagement_logs.removed_at IS NOT NULL` contract so duplicate deliveries cannot inflate active scores. |
| T-04-02 | R | `src/domain/scoring/reverse-engagement.ts` | mitigate | Model reversals as `removed_at` updates only; never delete engagement rows. |
| T-04-03 | D | `src/slack/register-events.ts` | mitigate | Bootstrap reaction handlers through one explicit registration point so listeners cannot be omitted silently from app startup. |
</threat_model>

<verification>
Run `npx vitest run tests/scoring/engagement-flow.test.ts` and `npm run typecheck`.
</verification>

<success_criteria>
Phase 1 scoring is deterministic under duplicate reactions, reversible under reaction removal, and actually reachable from the runtime bootstrap without ad hoc listener registration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-slack-core-trusted-scoring/01-04-SUMMARY.md`
</output>
