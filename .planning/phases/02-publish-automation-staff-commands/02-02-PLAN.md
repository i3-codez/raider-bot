---
phase: 02-publish-automation-staff-commands
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/config/env.ts
  - .env.example
  - src/domain/scoring/claim-engagement.ts
  - src/db/queries/find-raid-by-slack-ref.ts
  - src/db/queries/team-members.ts
  - src/db/queries/team-member-owner-aliases.ts
  - src/db/queries/update-raid-owner-slack-user.ts
  - src/domain/roster/resolve-post-owner.ts
  - supabase/migrations/20260411051000_phase2_team_roster.sql
  - tests/roster/self-raid-exclusion.test.ts
autonomous: true
requirements:
  - ENG-07
  - TEAM-01
must_haves:
  truths:
    - "Slack user IDs map to active leaderboard display names through durable roster data."
    - "Self-raid exclusion applies only when enabled and when owner mapping is confident."
    - "The linked Supabase project is updated with roster and owner-mapping schema before verification finishes."
  artifacts:
    - path: src/db/queries/team-members.ts
      provides: "Canonical roster reads and writes keyed by Slack user ID"
      exports:
        - upsertTeamMember
        - findTeamMemberBySlackUserId
    - path: src/domain/roster/resolve-post-owner.ts
      provides: "Owner alias resolution for incoming raid ownership metadata"
      exports:
        - resolvePostOwner
    - path: src/db/queries/update-raid-owner-slack-user.ts
      provides: "Backfill persistence for confident raid owner Slack-user matches"
      exports:
        - updateRaidOwnerSlackUser
    - path: supabase/migrations/20260411051000_phase2_team_roster.sql
      provides: "Roster and owner-mapping schema"
      contains: "create table team_members"
    - path: tests/roster/self-raid-exclusion.test.ts
      provides: "Self-raid exclusion and roster mapping coverage"
      contains: "RAIDER_EXCLUDE_SELF_RAIDS"
  key_links:
    - from: src/db/queries/find-raid-by-slack-ref.ts
      to: src/domain/scoring/claim-engagement.ts
      via: "owner-aware raid lookup informs self-raid exclusion"
      pattern: "owner"
    - from: src/domain/scoring/claim-engagement.ts
      to: src/domain/roster/resolve-post-owner.ts
      via: "confident owner mapping and owner-ID backfill before score persistence"
      pattern: "resolvePostOwner\\("
    - from: src/domain/roster/resolve-post-owner.ts
      to: src/db/queries/team-member-owner-aliases.ts
      via: "alias lookup drives owner matching"
      pattern: "team_member_owner_aliases"
    - from: src/domain/roster/resolve-post-owner.ts
      to: src/db/queries/update-raid-owner-slack-user.ts
      via: "confident owner match persisted onto raid_posts"
      pattern: "updateRaidOwnerSlackUser\\("
---

<objective>
Add the roster and ownership layer needed for leaderboard identity plus optional self-raid exclusion, without changing the canonical Phase 1 scoring and audit model.

Purpose: Leaderboards and owner-aware scoring need durable staff identity and post-owner mapping before command surfaces or exclusion rules can behave predictably.
Output: roster schema and query layer, owner-resolution service, self-raid exclusion toggle in scoring, and tests for confident-match-only behavior.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md
@.planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement roster storage, owner resolution, and self-raid exclusion rules</name>
  <files>src/config/env.ts, .env.example, src/domain/scoring/claim-engagement.ts, src/db/queries/find-raid-by-slack-ref.ts, src/db/queries/team-members.ts, src/db/queries/team-member-owner-aliases.ts, src/db/queries/update-raid-owner-slack-user.ts, src/domain/roster/resolve-post-owner.ts, supabase/migrations/20260411051000_phase2_team_roster.sql, tests/roster/self-raid-exclusion.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md, .planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md, src/domain/scoring/claim-engagement.ts, src/db/queries/find-raid-by-slack-ref.ts, src/db/queries/insert-raid-post.ts, supabase/migrations/20260410213000_phase1_core.sql, supabase/migrations/20260411050000_phase2_publish_webhook.sql</read_first>
  <behavior>
    - Test 1: roster members are keyed by Slack user ID and expose an active display name for reporting.
    - Test 2: owner aliases can resolve an incoming post owner to a known roster member.
    - Test 3: when roster aliases produce a confident owner match, that match is persisted onto `raid_posts.owner_slack_user_id` before self-raid scoring checks run.
    - Test 4: when `RAIDER_EXCLUDE_SELF_RAIDS=true` and the raid record contains a confident `owner_slack_user_id`, the matching owner does not receive a new score for their own raid.
    - Test 5: when the toggle is disabled or owner mapping is ambiguous or absent, the score path behaves like Phase 1 and still records the engagement.
  </behavior>
  <action>Add `RAIDER_EXCLUDE_SELF_RAIDS` to `src/config/env.ts` and `.env.example` as a boolean toggle defaulting to `false`. Create `supabase/migrations/20260411051000_phase2_team_roster.sql` with `team_members` keyed by `slack_user_id`, including `display_name`, `is_active`, timestamps, and a companion `team_member_owner_aliases` table that stores normalized owner identifiers per member. Add `src/db/queries/team-members.ts` and `src/db/queries/team-member-owner-aliases.ts` for roster and alias lookups. Create `src/domain/roster/resolve-post-owner.ts` exporting `resolvePostOwner(...)`, which normalizes incoming owner data and returns a confident team-member match only when exactly one active alias match exists. Add `src/db/queries/update-raid-owner-slack-user.ts` exporting `updateRaidOwnerSlackUser(raidPostId, ownerSlackUserId)` and use it in an explicit owner-backfill path: when a raid record has `owner_external_id` or `owner_display_name` from Plan 02-01 and `owner_slack_user_id IS NULL`, resolve the owner through roster aliases and persist the confident match onto `raid_posts.owner_slack_user_id`. Extend `src/db/queries/find-raid-by-slack-ref.ts` to return `owner_external_id`, `owner_display_name`, and `owner_slack_user_id` from the raid record. Update `src/domain/scoring/claim-engagement.ts` so before calling the persistence layer it checks `RAIDER_EXCLUDE_SELF_RAIDS`; if the toggle is `true` and the reacting Slack user matches the raid's confidently stored `owner_slack_user_id`, return without calling `claimEngagementLog(...)`. Add `tests/roster/self-raid-exclusion.test.ts` covering owner backfill from alias resolution, confident exclusion from persisted owner identity, disabled toggle behavior, and ambiguous-alias resolution that refuses to backfill `owner_slack_user_id` when more than one active alias matches.</action>
  <verify>
    <automated>npx vitest run tests/roster/self-raid-exclusion.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/env.ts` and `.env.example` contain `RAIDER_EXCLUDE_SELF_RAIDS`.
    - `supabase/migrations/20260411051000_phase2_team_roster.sql` contains `create table team_members` and `create table team_member_owner_aliases`.
    - `src/domain/roster/resolve-post-owner.ts` exports `resolvePostOwner`.
    - `src/db/queries/update-raid-owner-slack-user.ts` exports `updateRaidOwnerSlackUser`.
    - `src/db/queries/find-raid-by-slack-ref.ts` returns `owner_slack_user_id`.
    - `src/domain/scoring/claim-engagement.ts` contains `RAIDER_EXCLUDE_SELF_RAIDS` and a comparison between the reacting Slack user and `owner_slack_user_id`.
    - `tests/roster/self-raid-exclusion.test.ts` includes owner-backfill coverage plus both exclusion-on and exclusion-off scenarios.
    - `npx vitest run tests/roster/self-raid-exclusion.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Roster identity is durable, owner mapping is explicit and auditable, and self-raid exclusion only fires when the configured rule can be applied with confidence.</done>
</task>

<task type="auto" tdd="false">
  <name>[BLOCKING] Task 2: Push the Phase 2 roster schema to linked Supabase</name>
  <files>supabase/migrations/20260411051000_phase2_team_roster.sql</files>
  <read_first>supabase/migrations/20260411050000_phase2_publish_webhook.sql, supabase/migrations/20260411051000_phase2_team_roster.sql, .planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md</read_first>
  <behavior>
    - The linked Supabase project reflects the committed roster and owner-mapping schema before later plans depend on it.
  </behavior>
  <action>After `supabase/migrations/20260411051000_phase2_team_roster.sql` is complete, run the blocking command `npx supabase db push --linked`. If the push fails, fix the migration before moving on. Do not treat test success as a substitute for applying the schema to the linked database.</action>
  <verify>
    <automated>npx supabase db push --linked</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push --linked` exits `0`.
    - The push runs after `supabase/migrations/20260411051000_phase2_team_roster.sql` exists.
  </acceptance_criteria>
  <done>The linked Supabase project includes the roster and owner-mapping schema needed for self-raid-aware scoring and reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Roster data -> scoring service | Team identity and owner mappings affect point eligibility |
| Incoming owner metadata -> alias resolution | Ambiguous data must not suppress valid engagement claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | T | owner alias resolution | mitigate | Only treat a match as confident when exactly one active alias resolves to one team member. |
| T-02-05 | I | `/mystats` and roster identity | mitigate | Keep personal identity mapping in durable roster tables instead of deriving it ad hoc from Slack payloads. |
| T-02-06 | E | self-raid exclusion toggle | mitigate | Gate self-raid suppression behind `RAIDER_EXCLUDE_SELF_RAIDS` so the rule is explicit and reversible. |
</threat_model>

<verification>
Run `npx vitest run tests/roster/self-raid-exclusion.test.ts`, `npm run typecheck`, and `npx supabase db push --linked`.
</verification>

<success_criteria>
Team identity and owner matching are durable and auditable, and self-raid exclusion is predictable instead of relying on weak guesses or overloaded Phase 1 fields.
</success_criteria>

<output>
After completion, create `.planning/phases/02-publish-automation-staff-commands/02-02-SUMMARY.md`
</output>
