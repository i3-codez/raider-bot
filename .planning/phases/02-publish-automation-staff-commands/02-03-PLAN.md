---
phase: 02-publish-automation-staff-commands
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - slack/app-manifest.yml
  - src/slack/register-commands.ts
  - src/slack/commands/register-leaderboard-command.ts
  - src/slack/commands/register-mystats-command.ts
  - src/slack/commands/register-raiderhelp-command.ts
  - src/slack/blocks/build-leaderboard-message.ts
  - src/domain/reporting/get-monthly-leaderboard.ts
  - src/domain/reporting/get-member-monthly-stats.ts
  - src/db/queries/monthly-leaderboard.ts
  - src/db/queries/member-monthly-stats.ts
  - tests/reporting/monthly-leaderboard.test.ts
  - tests/slack/staff-commands.test.ts
autonomous: true
requirements:
  - TEAM-02
  - TEAM-03
  - TEAM-04
must_haves:
  truths:
    - "`/leaderboard`, `/mystats`, and `/raiderhelp` all read from the canonical scoring model rather than custom per-command logic."
    - "`/leaderboard` is intentionally shared, while `/mystats` and `/raiderhelp` stay private."
    - "The Slack manifest and command registration are updated so the new command surfaces are actually reachable."
  artifacts:
    - path: src/domain/reporting/get-monthly-leaderboard.ts
      provides: "Canonical monthly leaderboard service"
      exports:
        - getMonthlyLeaderboard
    - path: src/domain/reporting/get-member-monthly-stats.ts
      provides: "Canonical monthly personal-stats service"
      exports:
        - getMemberMonthlyStats
    - path: src/slack/commands/register-mystats-command.ts
      provides: "Private `/mystats` command handler"
      contains: "/mystats"
    - path: src/slack/commands/register-raiderhelp-command.ts
      provides: "Private `/raiderhelp` command handler"
      contains: "/raiderhelp"
  key_links:
    - from: src/slack/register-commands.ts
      to: src/slack/commands/register-leaderboard-command.ts
      via: "command bootstrap"
      pattern: "registerLeaderboardCommand\\(app\\)"
    - from: src/slack/commands/register-leaderboard-command.ts
      to: src/domain/reporting/get-monthly-leaderboard.ts
      via: "shared leaderboard query service"
      pattern: "getMonthlyLeaderboard\\("
    - from: src/slack/commands/register-mystats-command.ts
      to: src/domain/reporting/get-member-monthly-stats.ts
      via: "shared member stats service"
      pattern: "getMemberMonthlyStats\\("
    - from: src/slack/commands/register-raiderhelp-command.ts
      to: src/domain/scoring/action-registry.ts
      via: "help content uses canonical action registry"
      pattern: "ACTION_REGISTRY"
---

<objective>
Add the Phase 2 staff-command surfaces and reusable reporting queries so staff can view the current monthly leaderboard, receive private personal stats, and ask for help without diverging from the canonical scoring model.

Purpose: Phase 2 is only complete when the automated data can be surfaced back to staff through usable Slack commands, not just stored in the database.
Output: reporting query layer, Slack command handlers and registration, leaderboard/help block builders, manifest updates, and tests for command privacy plus output correctness.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md
@.planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement reusable reporting queries and shared leaderboard or stats formatting</name>
  <files>src/domain/reporting/get-monthly-leaderboard.ts, src/domain/reporting/get-member-monthly-stats.ts, src/db/queries/monthly-leaderboard.ts, src/db/queries/member-monthly-stats.ts, src/slack/blocks/build-leaderboard-message.ts, tests/reporting/monthly-leaderboard.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md, .planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts, src/db/queries/engagement-logs.ts, src/lib/time.ts, supabase/migrations/20260410213000_phase1_core.sql, supabase/migrations/20260411050000_phase2_publish_webhook.sql, supabase/migrations/20260411051000_phase2_team_roster.sql</read_first>
  <behavior>
    - Test 1: monthly leaderboard rows aggregate only active engagement claims and group them by roster display name.
    - Test 2: personal monthly stats derive from the same canonical query inputs as the shared leaderboard and include at least total points, unique raids engaged, and early-window actions.
    - Test 3: help content uses the canonical emoji registry and scoring windows rather than hand-entered strings.
  </behavior>
  <action>Create `src/db/queries/monthly-leaderboard.ts` and `src/db/queries/member-monthly-stats.ts` so both aggregate from `engagement_logs` joined to `raid_posts`, filter out rows where `removed_at IS NOT NULL`, scope to the current `month_key`, and map Slack users through active roster display names when available. Create `src/domain/reporting/get-monthly-leaderboard.ts` exporting `getMonthlyLeaderboard(...)` and `src/domain/reporting/get-member-monthly-stats.ts` exporting `getMemberMonthlyStats(...)` as thin service wrappers over those queries. Add `src/slack/blocks/build-leaderboard-message.ts` that formats a concise top-line leaderboard payload and a reusable stats section for command responses. Keep help content sourced from `ACTION_REGISTRY` and `SCORING_WINDOWS` so the command copy matches live scoring behavior exactly. Add `tests/reporting/monthly-leaderboard.test.ts` covering removed-score exclusion, roster display-name mapping, and personal stat totals derived from the same canonical scoring data.</action>
  <verify>
    <automated>npx vitest run tests/reporting/monthly-leaderboard.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/reporting/get-monthly-leaderboard.ts` exports `getMonthlyLeaderboard`.
    - `src/domain/reporting/get-member-monthly-stats.ts` exports `getMemberMonthlyStats`.
    - `src/db/queries/monthly-leaderboard.ts` and `src/db/queries/member-monthly-stats.ts` filter out `removed_at IS NOT NULL`.
    - `src/slack/blocks/build-leaderboard-message.ts` contains leaderboard rendering logic.
    - `tests/reporting/monthly-leaderboard.test.ts` covers removed-score exclusion and roster display names.
    - `npx vitest run tests/reporting/monthly-leaderboard.test.ts` exits `0`.
  </acceptance_criteria>
  <done>The project has one reusable reporting layer that can serve shared leaderboard and private personal-stats output without duplicating scoring logic per command.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register `/leaderboard`, `/mystats`, and `/raiderhelp` with the correct Slack visibility</name>
  <files>slack/app-manifest.yml, src/slack/register-commands.ts, src/slack/commands/register-leaderboard-command.ts, src/slack/commands/register-mystats-command.ts, src/slack/commands/register-raiderhelp-command.ts, tests/slack/staff-commands.test.ts</files>
  <read_first>AGENTS.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md, src/slack/register-commands.ts, src/slack/commands/register-raid-command.ts, src/slack/commands/handle-raid-submit.ts, slack/app-manifest.yml, src/domain/reporting/get-monthly-leaderboard.ts, src/domain/reporting/get-member-monthly-stats.ts, src/domain/scoring/action-registry.ts, src/domain/scoring/scoring-config.ts</read_first>
  <behavior>
    - Test 1: `registerCommands(app)` registers `/leaderboard`, `/mystats`, and `/raiderhelp` in addition to the existing `/raid` flow.
    - Test 2: `/leaderboard` returns a shared monthly ranking payload suitable for channel visibility.
    - Test 3: `/mystats` opens or reuses a DM path and keeps personal stats private to the requesting user.
    - Test 4: `/raiderhelp` responds privately and explains the emoji mapping plus scoring windows from the canonical config.
  </behavior>
  <action>Update `slack/app-manifest.yml` to declare slash commands `/leaderboard`, `/mystats`, and `/raiderhelp` alongside `/raid`. Add `src/slack/commands/register-leaderboard-command.ts`, `src/slack/commands/register-mystats-command.ts`, and `src/slack/commands/register-raiderhelp-command.ts`, each following the existing thin-handler pattern from the `/raid` command registration flow. Update `src/slack/register-commands.ts` so it registers all three new handlers. `/leaderboard` must call `getMonthlyLeaderboard(...)` and respond with a shared leaderboard payload. `/mystats` must call `getMemberMonthlyStats(...)`, open or reuse a DM channel for the requesting user, and send the result privately. `/raiderhelp` must build its guidance from `ACTION_REGISTRY` and `SCORING_WINDOWS` and return privately rather than posting public help spam in the raid channel. Add `tests/slack/staff-commands.test.ts` asserting bootstrap registration for all commands, shared-vs-private response behavior, and canonical help-content sourcing.</action>
  <verify>
    <automated>npx vitest run tests/slack/staff-commands.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `slack/app-manifest.yml` contains `/leaderboard`, `/mystats`, and `/raiderhelp`.
    - `src/slack/register-commands.ts` contains `registerLeaderboardCommand(app)`, `registerMystatsCommand(app)`, and `registerRaiderhelpCommand(app)`.
    - `src/slack/commands/register-mystats-command.ts` contains DM-specific Slack client usage.
    - `src/slack/commands/register-raiderhelp-command.ts` references `ACTION_REGISTRY` and `SCORING_WINDOWS`.
    - `tests/slack/staff-commands.test.ts` asserts private handling for `/mystats` and `/raiderhelp`.
    - `npx vitest run tests/slack/staff-commands.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Staff command surfaces are actually reachable from Slack, respect the planned privacy boundaries, and reflect the same canonical scoring model as the rest of the product.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slash commands -> reporting services | Staff-facing outputs must not drift from the canonical scoring model |
| Shared leaderboard -> Slack channel | Public output should avoid leaking private user-specific details |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | I | `/mystats` | mitigate | Deliver personal stats through DM only and keep the handler separate from shared leaderboard responses. |
| T-02-08 | T | `/raiderhelp` content | mitigate | Generate help copy from `ACTION_REGISTRY` and `SCORING_WINDOWS` so docs cannot drift from scoring behavior. |
| T-02-09 | R | monthly leaderboard queries | mitigate | Derive command output from `engagement_logs` and `raid_posts`, not hand-maintained totals. |
</threat_model>

<verification>
Run `npx vitest run tests/reporting/monthly-leaderboard.test.ts`, `npx vitest run tests/slack/staff-commands.test.ts`, and `npm run typecheck`.
</verification>

<success_criteria>
Staff can access accurate shared leaderboard and private personal-stats or help surfaces from Slack, and those outputs stay anchored to the same canonical scoring data the rest of Raider Bot uses.
</success_criteria>

<output>
After completion, create `.planning/phases/02-publish-automation-staff-commands/02-03-SUMMARY.md`
</output>
