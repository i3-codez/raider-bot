---
phase: 02-publish-automation-staff-commands
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/server.ts
  - src/config/env.ts
  - .env.example
  - src/domain/raids/create-raid.ts
  - src/domain/raids/create-manual-raid.ts
  - src/db/queries/insert-raid-post.ts
  - src/db/queries/find-raid-by-dedupe-key.ts
  - supabase/migrations/20260411050000_phase2_publish_webhook.sql
  - tests/raids/publish-webhook.test.ts
autonomous: true
requirements:
  - RAID-02
  - RAID-04
user_setup:
  - service: webhook
    why: "Authenticated publish automation requires a shared secret known to the upstream workflow."
    env_vars:
      - name: PUBLISH_WEBHOOK_SHARED_SECRET
        source: "Shared secret chosen for the publishing workflow and configured in the Raider Bot runtime"
must_haves:
  truths:
    - "An authenticated publish webhook can create a raid using authoritative `published_at` without duplicating the posting flow."
    - "Repeated publish requests resolve idempotently instead of creating duplicate raid posts."
    - "The linked Supabase project is updated with the Phase 2 webhook and dedupe schema before verification finishes."
  artifacts:
    - path: src/domain/raids/create-raid.ts
      provides: "Canonical shared raid-ingestion service for manual and webhook flows"
      exports:
        - createRaid
    - path: src/db/queries/find-raid-by-dedupe-key.ts
      provides: "Dedupe lookup for upstream event IDs and normalized post URLs"
      exports:
        - findRaidByDedupeKey
    - path: supabase/migrations/20260411050000_phase2_publish_webhook.sql
      provides: "Webhook and dedupe schema extension"
      contains: "normalized_post_url"
    - path: tests/raids/publish-webhook.test.ts
      provides: "Webhook validation and duplicate-submit coverage"
      contains: "PUBLISH_WEBHOOK_SHARED_SECRET"
  key_links:
    - from: src/app/server.ts
      to: src/domain/raids/create-raid.ts
      via: "validated webhook handler delegates to shared raid ingestion"
      pattern: "createRaid\\("
    - from: src/domain/raids/create-manual-raid.ts
      to: src/domain/raids/create-raid.ts
      via: "manual raid flow reuses shared ingestion service"
      pattern: "createRaid\\("
    - from: src/domain/raids/create-raid.ts
      to: src/db/queries/find-raid-by-dedupe-key.ts
      via: "duplicate publish protection"
      pattern: "findRaidByDedupeKey\\("
    - from: src/domain/raids/create-raid.ts
      to: src/db/queries/insert-raid-post.ts
      via: "canonical raid persistence"
      pattern: "insertRaidPost\\("
---

<objective>
Add the publish-automation foundation: an authenticated webhook entry point, a transport-agnostic raid creation service shared with the existing manual flow, and durable dedupe support so duplicate publish events do not create second raid posts.

Purpose: Phase 2 starts by replacing manual timing gaps with authoritative automated ingest while preserving the Phase 1 raid record and Slack message contracts.
Output: webhook runtime wiring, shared raid-ingestion service, dedupe query support, schema updates for idempotency metadata, and tests for validation plus duplicate-submit behavior.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md
@.planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md
@.planning/phases/01-slack-core-trusted-scoring/01-CONTEXT.md
@.planning/phases/01-slack-core-trusted-scoring/01-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement authenticated webhook ingest on the canonical raid creation path</name>
  <files>src/app/server.ts, src/config/env.ts, .env.example, src/domain/raids/create-raid.ts, src/domain/raids/create-manual-raid.ts, src/db/queries/insert-raid-post.ts, src/db/queries/find-raid-by-dedupe-key.ts, supabase/migrations/20260411050000_phase2_publish_webhook.sql, tests/raids/publish-webhook.test.ts</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/02-publish-automation-staff-commands/02-CONTEXT.md, .planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md, src/app/server.ts, src/app/slack.ts, src/config/env.ts, .env.example, src/domain/raids/create-manual-raid.ts, src/db/queries/insert-raid-post.ts, src/domain/raids/correct-raid-published-at.ts, supabase/migrations/20260410213000_phase1_core.sql</read_first>
  <behavior>
    - Test 1: an authenticated webhook request with `post_url`, `client_name`, `platform`, and `published_at` creates a raid through the canonical service and persists a high-confidence raid row.
    - Test 2: a webhook request missing `published_at` fails validation instead of silently creating a low-confidence automated raid.
    - Test 3: a repeated webhook request with the same upstream event key or normalized `(platform, post_url)` resolves idempotently and does not create a second Slack post or second raid row.
    - Test 4: the existing manual raid path still works by delegating to the new shared `createRaid(...)` service instead of forking behavior.
  </behavior>
  <action>Create `src/domain/raids/create-raid.ts` exporting `createRaid(input, context)` as the shared ingestion path for both manual and webhook flows. The shared service must accept explicit `publishedAt`, `postUrl`, `clientName`, `platform`, `createdBySlackUserId`, and optional dedupe metadata, derive `timing_confidence = "high"` for webhook payloads with authoritative publish time, derive `month_key` from the effective scoring timestamp, build the canonical raid message, and persist the raid through `insertRaidPost(...)`. Update `src/domain/raids/create-manual-raid.ts` so manual slash-command creation becomes a thin wrapper over `createRaid(...)` and preserves the existing Phase 1 fallback semantics when `publishedAt` is omitted. Add `src/db/queries/find-raid-by-dedupe-key.ts` to resolve existing raids by a stored upstream idempotency key when present and by normalized `(platform, post_url)` otherwise. Extend `src/db/queries/insert-raid-post.ts` and add `supabase/migrations/20260411050000_phase2_publish_webhook.sql` so `raid_posts` gains `normalized_post_url` and nullable `source_event_id` fields plus unique indexes on `(platform, normalized_post_url)` and `source_event_id` when present. In `src/config/env.ts` and `.env.example`, add `PUBLISH_WEBHOOK_SHARED_SECRET`. In `src/app/server.ts`, add a dedicated authenticated POST webhook route such as `/publish/webhook` that validates the shared-secret header, parses the required fields with `zod`, redacts secrets from logs, and delegates to `createRaid(...)`. If an incoming webhook matches an existing low-confidence manual raid for the same normalized post URL, reuse the existing raid and update `published_at` through the existing timing-correction path instead of creating a duplicate post. Add `tests/raids/publish-webhook.test.ts` covering valid create, missing-`published_at` rejection, duplicate retry behavior, and manual-path reuse.</action>
  <verify>
    <automated>npx vitest run tests/raids/publish-webhook.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/config/env.ts` and `.env.example` contain `PUBLISH_WEBHOOK_SHARED_SECRET`.
    - `src/app/server.ts` contains the exact route path `/publish/webhook`.
    - `src/domain/raids/create-raid.ts` exports `createRaid`.
    - `src/domain/raids/create-manual-raid.ts` contains `createRaid(`.
    - `supabase/migrations/20260411050000_phase2_publish_webhook.sql` contains `normalized_post_url`, `source_event_id`, `unique index`, and `(platform, normalized_post_url)`.
    - `tests/raids/publish-webhook.test.ts` includes duplicate-submit coverage and missing-`published_at` rejection coverage.
    - `npx vitest run tests/raids/publish-webhook.test.ts` exits `0`.
  </acceptance_criteria>
  <done>The service accepts authenticated publish events, creates or upgrades canonical raids from authoritative publish data, and safely suppresses duplicate publish retries.</done>
</task>

<task type="auto" tdd="false">
  <name>[BLOCKING] Task 2: Push the Phase 2 webhook schema to linked Supabase</name>
  <files>supabase/migrations/20260411050000_phase2_publish_webhook.sql</files>
  <read_first>supabase/migrations/20260410213000_phase1_core.sql, supabase/migrations/20260411050000_phase2_publish_webhook.sql, .planning/phases/02-publish-automation-staff-commands/02-RESEARCH.md</read_first>
  <behavior>
    - The linked Supabase project reflects the committed webhook and dedupe schema before plan verification completes.
  </behavior>
  <action>After all schema file modifications in `supabase/migrations/20260411050000_phase2_publish_webhook.sql` are complete and committed in the workspace, run the blocking command `npx supabase db push --linked`. If the push reports schema errors, stop and fix the migration before continuing. Do not skip this step just because tests or typecheck pass; the live linked database must match the committed SQL before downstream plans run.</action>
  <verify>
    <automated>npx supabase db push --linked</automated>
  </verify>
  <acceptance_criteria>
    - `npx supabase db push --linked` exits `0`.
    - The push runs after `supabase/migrations/20260411050000_phase2_publish_webhook.sql` exists.
  </acceptance_criteria>
  <done>The linked Supabase project has the Phase 2 webhook and dedupe schema applied, eliminating schema drift before subsequent plans build on it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Publishing workflow -> webhook endpoint | External requests must be authenticated and validated before creating raids |
| Webhook service -> raid_posts | Idempotency and canonical raid identity are enforced here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | S | `/publish/webhook` | mitigate | Require `PUBLISH_WEBHOOK_SHARED_SECRET` and reject requests missing the expected auth header. |
| T-02-02 | T | `raid_posts` dedupe fields | mitigate | Add durable unique indexes for `source_event_id` and normalized `(platform, post_url)` matching before webhook traffic is trusted. |
| T-02-03 | R | timing upgrades for manual raids | mitigate | Reuse the existing timing-correction path so authoritative webhook upgrades remain auditable instead of overwriting history silently. |
</threat_model>

<verification>
Run `npx vitest run tests/raids/publish-webhook.test.ts`, `npm run typecheck`, and `npx supabase db push --linked`.
</verification>

<success_criteria>
Publish automation enters through one authenticated path, uses the same canonical raid model as manual creation, and prevents duplicate publish retries from creating duplicate raids.
</success_criteria>

<output>
After completion, create `.planning/phases/02-publish-automation-staff-commands/02-01-SUMMARY.md`
</output>
