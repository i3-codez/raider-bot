---
phase: 03-reporting-reminders-launch-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/config/env.ts
  - src/lib/time.ts
  - src/db/queries/summary-reporting.ts
  - src/domain/reporting/summary-reporting.ts
  - src/slack/blocks/build-summary-message.ts
  - src/slack/client.ts
  - src/jobs/publish-summary.ts
  - src/scripts/run-summary-job.ts
  - tests/reporting/summary-reporting.test.ts
  - tests/jobs/publish-summary-job.test.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-03
must_haves:
  truths:
    - "Daily, weekly, and monthly summary jobs all read from one canonical reporting layer rather than per-job custom math."
    - "Summary payloads include total points, unique raid posts engaged, early-window actions, and early-window action rate."
    - "The scheduled summary path supports dry-run inspection and summary-channel fallback for pilot rollout."
  artifacts:
    - path: src/domain/reporting/summary-reporting.ts
      provides: "Canonical ET-aware summary reporting service"
      exports:
        - getSummaryReport
    - path: src/jobs/publish-summary.ts
      provides: "Summary job orchestration for daily, weekly, and monthly posting"
      contains: "publishSummaryJob"
    - path: src/scripts/run-summary-job.ts
      provides: "Scheduler-safe summary job entrypoint with dry-run support"
      contains: "runSummaryJobCommand"
  key_links:
    - from: src/jobs/publish-summary.ts
      to: src/domain/reporting/summary-reporting.ts
      via: "shared summary service"
      pattern: "getSummaryReport\\("
    - from: src/domain/reporting/summary-reporting.ts
      to: src/db/queries/summary-reporting.ts
      via: "canonical SQL-backed reporting"
      pattern: "querySummaryReport\\("
    - from: src/jobs/publish-summary.ts
      to: src/slack/blocks/build-summary-message.ts
      via: "one reusable Slack summary formatter"
      pattern: "buildSummaryMessage\\("
---

<objective>
Add the Phase 3 reusable summary-reporting layer and scheduled summary job entrypoints so Raider Bot can publish daily, weekly, and monthly leaderboard summaries from one canonical ET-aware metric model.

Purpose: Phase 3 cannot succeed if scheduled summaries are implemented as isolated cron scripts with duplicated math. This plan establishes one reporting contract, one Slack formatter, and one scheduler-facing entrypoint for summary publication.
Output: ET-aware reporting utilities, canonical summary queries and service, reusable Slack summary message builder, summary job orchestration, env and script wiring, and tests for metrics plus dry-run behavior.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-RESEARCH.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend ET-aware reporting into reusable daily, weekly, and monthly summary data</name>
  <files>src/lib/time.ts, src/db/queries/summary-reporting.ts, src/domain/reporting/summary-reporting.ts, tests/reporting/summary-reporting.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, .planning/phases/03-reporting-reminders-launch-hardening/03-RESEARCH.md, src/lib/time.ts, src/domain/reporting/monthly-reporting.ts, src/db/queries/monthly-reporting.ts, tests/reporting/monthly-leaderboard.test.ts</read_first>
  <behavior>
    - Test 1: summary reporting derives the correct ET daily, weekly, and monthly window boundaries, including Monday-based weekly windows.
    - Test 2: summary metrics include total points, unique raids engaged, early-window actions, total actions, and a derived early-window action rate with `0` for zero-action windows.
    - Test 3: reporting accepts injected `now` and query dependencies so job and pilot dry-run tests remain deterministic.
  </behavior>
  <action>Extend `src/lib/time.ts` with ET-aware report-window helpers for day, week, and month windows, preserving the existing month-key behavior as the canonical source of truth. Add `src/db/queries/summary-reporting.ts` that aggregates canonical summary rows over explicit date ranges rather than only the current month key, while still sourcing data from `engagement_logs` joined to `raid_posts` and excluding removed claims. Add `src/domain/reporting/summary-reporting.ts` exposing a shared `getSummaryReport(...)` service that accepts a cadence such as `daily`, `weekly`, or `monthly`, derives the correct ET window, and returns the metrics required by `RPT-03`, including `earlyWindowActionRate`. Add `tests/reporting/summary-reporting.test.ts` for ET boundary behavior, derived-rate math, and dependency injection.</action>
  <verify>
    <automated>npx vitest run tests/reporting/summary-reporting.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/reporting/summary-reporting.ts` exports a reusable summary service for daily, weekly, and monthly cadences.
    - `src/db/queries/summary-reporting.ts` reads from canonical raid and engagement data and excludes `removed_at IS NOT NULL`.
    - `tests/reporting/summary-reporting.test.ts` covers ET day or week boundaries and `earlyWindowActionRate`.
    - `npx vitest run tests/reporting/summary-reporting.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Raider Bot has one ET-aware summary reporting contract that scheduled jobs can trust for all launch cadences.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reusable summary Slack formatting and a scheduler-safe summary job entrypoint</name>
  <files>package.json, .env.example, src/config/env.ts, src/slack/blocks/build-summary-message.ts, src/slack/client.ts, src/jobs/publish-summary.ts, src/scripts/run-summary-job.ts, tests/jobs/publish-summary-job.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, src/app/slack.ts, src/app/server.ts, src/slack/blocks/build-raid-message.ts, src/domain/reporting/summary-reporting.ts, package.json, .env.example, src/config/env.ts</read_first>
  <behavior>
    - Test 1: the summary job uses one reusable Slack payload builder that returns fallback text plus blocks, mirroring the established raid-message pattern.
    - Test 2: the summary job posts to `SLACK_SUMMARY_CHANNEL_ID` when present and falls back to `SLACK_RAID_CHANNEL_ID` when it is not configured.
    - Test 3: `--dry-run` builds and prints the payload without posting to Slack.
  </behavior>
  <action>Add `src/slack/blocks/build-summary-message.ts` so scheduled summaries use one reusable Slack formatter instead of per-job hand-built text. Add a lightweight `src/slack/client.ts` bootstrap that creates a Slack Web API client for jobs and scripts without requiring the full Bolt HTTP receiver. Add `src/jobs/publish-summary.ts` that calls `getSummaryReport(...)`, formats the payload, honors summary-channel fallback, and skips posting when dry-run mode is active. Add `src/scripts/run-summary-job.ts` as the scheduler-safe entrypoint that accepts a cadence and `--dry-run`, then update `package.json`, `.env.example`, and `src/config/env.ts` with the summary-job scripts and summary-channel configuration. Add `tests/jobs/publish-summary-job.test.ts` covering channel fallback, dry-run behavior, and payload construction.</action>
  <verify>
    <automated>npx vitest run tests/jobs/publish-summary-job.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/jobs/publish-summary.ts` contains `publishSummaryJob`.
    - `src/scripts/run-summary-job.ts` exports `runSummaryJobCommand` and supports `daily`, `weekly`, and `monthly` execution plus `--dry-run`.
    - `.env.example` and `src/config/env.ts` expose summary-channel configuration without breaking existing env validation.
    - `tests/jobs/publish-summary-job.test.ts` covers dry-run and channel fallback.
    - `npx vitest run tests/jobs/publish-summary-job.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Scheduled summaries can be generated and validated safely through one reusable job path before any month-close or reminder work is layered on top.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Scheduled jobs -> reporting services | Job output must not drift from the canonical scoring model |
| Summary jobs -> Slack channels | Pilot rollout must not accidentally post to the wrong channel or spam during dry-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | Summary metric generation | mitigate | Derive all summary metrics from shared SQL-backed reporting services, not per-job custom math. |
| T-03-02 | I | Dry-run and channel routing | mitigate | Support `--dry-run` and explicit summary-channel fallback logic with tests. |
| T-03-03 | R | Scheduled Slack output | mitigate | Use one reusable Block Kit formatter with deterministic text output. |
</threat_model>

<verification>
Run `npx vitest run tests/reporting/summary-reporting.test.ts`, `npx vitest run tests/jobs/publish-summary-job.test.ts`, and `npm run typecheck`.
</verification>

<success_criteria>
Raider Bot can generate and optionally publish daily, weekly, and monthly summary payloads from one ET-aware canonical reporting model without duplicating scoring logic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-reminders-launch-hardening/03-01-SUMMARY.md`
</output>
