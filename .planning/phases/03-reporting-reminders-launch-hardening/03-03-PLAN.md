---
phase: 03-reporting-reminders-launch-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - package.json
  - docs/pilot-launch-runbook.md
  - src/scripts/run-pilot-check.ts
  - tests/scripts/pilot-check.test.ts
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-04
must_haves:
  truths:
    - "Pilot launch readiness is represented in runnable validation steps, not just prose."
    - "Ops can verify summary, month-close, and reminder behavior safely through dry-run commands before posting in a private pilot."
    - "Scheduler ownership and required runtime wiring are documented clearly enough for one operator to execute without guesswork."
  artifacts:
    - path: docs/pilot-launch-runbook.md
      provides: "Ops-facing pilot launch and Railway cron runbook"
      contains: "Railway cron"
    - path: src/scripts/run-pilot-check.ts
      provides: "Runnable pilot validation script that exercises dry-run jobs"
      contains: "runPilotCheck"
    - path: tests/scripts/pilot-check.test.ts
      provides: "Coverage for pilot validation flow and safety checks"
      contains: "pilot check"
  key_links:
    - from: src/scripts/run-pilot-check.ts
      to: src/scripts/run-summary-job.ts
      via: "dry-run summary validation"
      pattern: "runSummaryJobCommand"
    - from: src/scripts/run-pilot-check.ts
      to: src/scripts/run-month-close.ts
      via: "dry-run month-close validation"
      pattern: "runMonthCloseCommand"
    - from: src/scripts/run-pilot-check.ts
      to: src/scripts/run-ops-surfacing.ts
      via: "dry-run ops validation"
      pattern: "runOpsSurfacingCommand"
---

<objective>
Finish Phase 3 with pilot-facing runtime hardening and runnable launch documentation so Raider Bot can be validated in a private pilot with one clear scheduler owner and predictable dry-run checks.

Purpose: The code is not launch-ready until an operator can verify env setup, scheduler commands, and dry-run job behavior without reading source files or improvising shell commands.
Output: pilot runbook documentation, a runnable pilot-check script that exercises dry-run job flows, package-script wiring, and tests that keep the validation path from drifting.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-RESEARCH.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-01-PLAN.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-02-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a runnable pilot validation script that exercises all Phase 3 dry-run job paths</name>
  <files>package.json, src/scripts/run-pilot-check.ts, tests/scripts/pilot-check.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, src/scripts/run-summary-job.ts, src/scripts/run-month-close.ts, src/scripts/run-ops-surfacing.ts, package.json</read_first>
  <behavior>
    - Test 1: the pilot check runs summary, month-close, and ops-surfacing flows in dry-run mode rather than posting to Slack.
    - Test 2: pilot check output fails fast when a required Phase 3 script surface is unavailable.
    - Test 3: the package script exposes one predictable pilot validation command for operators.
  </behavior>
  <action>Add `src/scripts/run-pilot-check.ts` that orchestrates the existing Phase 3 scripts in dry-run mode and prints a concise success or failure report for operators. Keep the script thin: it should validate that the summary, month-close, and ops-surfacing dry-run paths can all execute with the current env and script wiring, then stop short of live posting. Update `package.json` with one stable pilot-check command. Add `tests/scripts/pilot-check.test.ts` covering dry-run orchestration and failure handling when required hooks are missing.</action>
  <verify>
    <automated>npx vitest run tests/scripts/pilot-check.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/scripts/run-pilot-check.ts` exercises summary, month-close, and ops dry-run paths.
    - `package.json` exposes one pilot-check script.
    - `tests/scripts/pilot-check.test.ts` covers success and missing-hook failure behavior.
    - `npx vitest run tests/scripts/pilot-check.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Operators have one command that validates the private-pilot job surfaces without sending live Slack traffic.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write the pilot launch runbook with Railway cron ownership and private-pilot validation steps</name>
  <files>docs/pilot-launch-runbook.md</files>
  <read_first>AGENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, package.json, .env.example, slack/app-manifest.yml</read_first>
  <behavior>
    - The runbook lists required env vars and the summary-channel or ops-channel fallback model.
    - The runbook names Railway cron as the scheduler owner and includes example commands for daily, weekly, monthly, month-close, ops-surfacing, and pilot-check execution.
    - The runbook includes a private-pilot UAT checklist covering Slack scopes, dry-run validation, and live-post verification.
  </behavior>
  <action>Create `docs/pilot-launch-runbook.md` with the Phase 3 ops-facing launch instructions. Document required env configuration, summary and ops channel fallback behavior, Railway cron ownership, example cron commands, dry-run and pilot-check commands, manifest or Slack-scope verification, and a private-pilot UAT checklist that validates summaries, month-close, and ops surfacing in a controlled channel before broader rollout.</action>
  <verify>
    <manual>Review the runbook against `package.json`, `.env.example`, and `slack/app-manifest.yml` so the documented commands and scopes match the repo.</manual>
  </verify>
  <acceptance_criteria>
    - `docs/pilot-launch-runbook.md` names Railway cron as the scheduler owner.
    - The runbook includes concrete commands that match package scripts and dry-run entrypoints.
    - The runbook includes a private pilot checklist covering env, scopes, and live verification steps.
  </acceptance_criteria>
  <done>Phase 3 is operationally legible: one operator can configure, dry-run, and validate the private pilot without guessing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator docs -> runtime commands | Launch instructions must not drift from actual package scripts or env expectations |
| Pilot validation -> live Slack posting | Dry-run checks must stay safe and clearly separated from live execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | R | Pilot documentation | mitigate | Tie runbook commands to actual package scripts and validation tests. |
| T-03-08 | I | Pilot dry-run flow | mitigate | Use one dedicated pilot-check script that explicitly runs all validation steps without posting live output. |
</threat_model>

<verification>
Run `npx vitest run tests/scripts/pilot-check.test.ts` and `npm run typecheck`, then manually verify the runbook matches `package.json`, `.env.example`, and `slack/app-manifest.yml`.
</verification>

<success_criteria>
Raider Bot can be validated in a private pilot with one documented scheduler owner, one pilot-check command, and a runbook that matches the live code and config surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-reminders-launch-hardening/03-03-SUMMARY.md`
</output>
