---
phase: 03-reporting-reminders-launch-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - package.json
  - supabase/migrations/20260411120000_phase3_reporting_ops.sql
  - src/db/queries/job-runs.ts
  - src/db/queries/monthly-snapshots.ts
  - src/db/queries/reminder-candidates.ts
  - src/domain/reporting/month-close.ts
  - src/domain/reminders/ops-surfacing.ts
  - src/jobs/run-month-close.ts
  - src/jobs/publish-ops-surfacing.ts
  - src/scripts/run-month-close.ts
  - src/scripts/run-ops-surfacing.ts
  - tests/reporting/month-close.test.ts
  - tests/reminders/ops-surfacing.test.ts
autonomous: true
requirements:
  - RPT-02
  - RPT-04
must_haves:
  truths:
    - "Month reset is logical and ET-aware: historical logs remain untouched while the completed month is snapshotted durably."
    - "Month-close and ops-surfacing jobs are idempotent and safe to rerun after cron retries or late timing corrections."
    - "Low-confidence timing and underperforming first-30-minute raids are surfaced through one tested ops path with durable dedupe keyed by `raid_post_id + alert_type + alert_window_key`."
  artifacts:
    - path: supabase/migrations/20260411120000_phase3_reporting_ops.sql
      provides: "Durable snapshot and job-run schema for Phase 3"
      contains: "monthly_score_snapshots"
    - path: src/domain/reporting/month-close.ts
      provides: "Month-close snapshot orchestration"
      contains: "runMonthClose"
    - path: src/domain/reminders/ops-surfacing.ts
      provides: "Ops reminder heuristics and surfacing decisions"
      contains: "buildOpsSurfacingDigest"
    - path: src/scripts/run-ops-surfacing.ts
      provides: "Scheduler-safe ops surfacing entrypoint"
      contains: "runOpsSurfacingCommand"
  key_links:
    - from: src/domain/reporting/month-close.ts
      to: src/db/queries/monthly-snapshots.ts
      via: "snapshot persistence"
      pattern: "upsertMonthlySnapshots\\("
    - from: src/jobs/run-month-close.ts
      to: src/domain/reporting/month-close.ts
      via: "month-close orchestration"
      pattern: "runMonthClose\\("
    - from: src/domain/reminders/ops-surfacing.ts
      to: src/db/queries/reminder-candidates.ts
      via: "candidate selection"
      pattern: "queryReminderCandidates\\("
    - from: src/jobs/publish-ops-surfacing.ts
      to: src/domain/reminders/ops-surfacing.ts
      via: "ops digest generation"
      pattern: "buildOpsSurfacingDigest\\("
---

<objective>
Add the Phase 3 month-close snapshotting and ops-surfacing layer so Raider Bot can preserve completed-month standings, open new ET reporting windows safely, and notify ops about low-confidence or weak early participation without duplicate noise.

Purpose: Summary jobs alone are not enough for launch. The pilot also needs durable month-close behavior and a trustworthy ops signal path for raids that require follow-up.
Output: schema and query support for snapshots plus job-run idempotency, month-close orchestration, reminder-candidate queries, ops digest posting, and tests for ET edges plus rerun safety.
</objective>

<execution_context>
@/Users/matthewbaggetta/.codex/get-shit-done/workflows/execute-plan.md
@/Users/matthewbaggetta/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-RESEARCH.md
@.planning/phases/03-reporting-reminders-launch-hardening/03-01-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable schema and services for month-close snapshots plus idempotent job runs</name>
  <files>supabase/migrations/20260411120000_phase3_reporting_ops.sql, src/db/queries/job-runs.ts, src/db/queries/monthly-snapshots.ts, src/domain/reporting/month-close.ts, src/jobs/run-month-close.ts, src/scripts/run-month-close.ts, tests/reporting/month-close.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, .planning/phases/03-reporting-reminders-launch-hardening/03-RESEARCH.md, src/domain/reporting/summary-reporting.ts, src/db/queries/monthly-reporting.ts, src/lib/time.ts, supabase/migrations/20260410213000_phase1_core.sql, supabase/migrations/20260411050000_phase2_publish_webhook.sql, supabase/migrations/20260411051000_phase2_team_roster.sql</read_first>
  <behavior>
    - Test 1: month-close snapshots preserve final per-member standings and aggregate metrics for a given ET month key without mutating canonical raid or engagement logs.
    - Test 2: rerunning the month-close job for the same month upserts existing snapshot rows and does not duplicate durable job-run state.
    - Test 3: the month-close script supports dry-run mode and a target month override for pilot validation.
  </behavior>
  <action>Add `supabase/migrations/20260411120000_phase3_reporting_ops.sql` with durable Phase 3 schema for month-close snapshots and idempotent job-run tracking, including indexes that support summary and snapshot access. Add `src/db/queries/job-runs.ts` and `src/db/queries/monthly-snapshots.ts` for keyed job-run persistence and snapshot upsert behavior. Add `src/domain/reporting/month-close.ts` so month-close derives the correct prior ET month, captures final standings from the canonical summary layer, and writes snapshots using rerunnable upserts. Add `src/jobs/run-month-close.ts` plus `src/scripts/run-month-close.ts` for cron and manual execution, including `--dry-run`, target-month support, and an exported `runMonthCloseCommand` surface for Wave 3 pilot validation. Add `tests/reporting/month-close.test.ts` for rerun safety and month-boundary behavior.</action>
  <verify>
    <automated>npx vitest run tests/reporting/month-close.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The Phase 3 migration creates durable snapshot and job-run tables.
    - `src/domain/reporting/month-close.ts` uses snapshot upserts rather than duplicate inserts.
    - `src/scripts/run-month-close.ts` exports `runMonthCloseCommand` and supports dry-run plus explicit month targeting.
    - `tests/reporting/month-close.test.ts` covers rerunnable month-close behavior.
    - `npx vitest run tests/reporting/month-close.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Raider Bot can close a completed month safely and preserve the final standings without deleting or rewriting history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add low-confidence and weak-participation ops surfacing with durable dedupe</name>
  <files>package.json, src/db/queries/reminder-candidates.ts, src/domain/reminders/ops-surfacing.ts, src/jobs/publish-ops-surfacing.ts, src/scripts/run-ops-surfacing.ts, tests/reminders/ops-surfacing.test.ts</files>
  <read_first>AGENTS.md, .planning/phases/03-reporting-reminders-launch-hardening/03-CONTEXT.md, src/domain/scoring/scoring-config.ts, src/domain/raids/types.ts, src/db/queries/find-raid-by-slack-ref.ts, src/db/queries/monthly-reporting.ts, src/jobs/publish-summary.ts, src/slack/client.ts</read_first>
  <behavior>
    - Test 1: low-confidence raids are always eligible for ops surfacing, even if participation is otherwise healthy.
    - Test 2: underperforming raids surface only when they are at least 20 minutes old and have fewer than 3 early-window actions.
    - Test 3: repeated job runs do not republish the same ops item for the same raid and alert type.
  </behavior>
  <action>Add `src/db/queries/reminder-candidates.ts` that derives ops candidates from canonical raid and engagement data, including raid age, early-window counts, and low-confidence timing flags without incorrectly filtering out cross-month raids near ET midnight. Extend the Phase 3 durable job state so ops dedupe is keyed by `raid_post_id + alert_type + alert_window_key`, not only by coarse job-window execution. Add `src/domain/reminders/ops-surfacing.ts` that applies the locked launch heuristic, groups the results into an actionable ops digest, and records or checks that fine-grained dedupe state before publishing. Add `src/jobs/publish-ops-surfacing.ts` plus `src/scripts/run-ops-surfacing.ts` for dry-run and live execution, with an exported `runOpsSurfacingCommand` surface for Wave 3 pilot validation, and update `package.json` with the new script entrypoint. Add `tests/reminders/ops-surfacing.test.ts` for threshold edges, low-confidence inclusion, cross-month eligibility, and rerun dedupe.</action>
  <verify>
    <automated>npx vitest run tests/reminders/ops-surfacing.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/reminders/ops-surfacing.ts` implements the 20-minute plus fewer-than-3 early actions heuristic.
    - `src/db/queries/reminder-candidates.ts` does not rely on current month-only filtering for eligibility.
    - `src/scripts/run-ops-surfacing.ts` exports `runOpsSurfacingCommand`.
    - `tests/reminders/ops-surfacing.test.ts` covers low-confidence surfacing and rerun dedupe keyed by raid plus alert type.
    - `npx vitest run tests/reminders/ops-surfacing.test.ts` exits `0`.
  </acceptance_criteria>
  <done>Ops can see which raids need follow-up without duplicate reminder noise or timezone bugs around ET midnight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Month-close jobs -> snapshot tables | Historical reporting must remain accurate and rerunnable |
| Ops-surfacing jobs -> Slack channels | Reminder logic must avoid duplicate posts and false positives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | T | Month-close persistence | mitigate | Use durable job-run keys and snapshot upserts so reruns refresh state without duplicating rows. |
| T-03-05 | I | Ops reminder posting | mitigate | Persist dedupe state per job window and alert type before reposting. |
| T-03-06 | R | Reminder candidate selection | mitigate | Base eligibility on canonical raid plus engagement data and test ET edge cases explicitly. |
</threat_model>

<verification>
Run `npx vitest run tests/reporting/month-close.test.ts`, `npx vitest run tests/reminders/ops-surfacing.test.ts`, and `npm run typecheck`.
</verification>

<success_criteria>
Raider Bot can safely close a month, preserve final standings, and surface low-confidence or weak-participation raids to ops without duplicate cron noise.
</success_criteria>

<output>
After completion, create `.planning/phases/03-reporting-reminders-launch-hardening/03-02-SUMMARY.md`
</output>
